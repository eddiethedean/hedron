# Alpine.js 3.16.3 vendored assets

These browser files are the exact `dist/cdn.min.js` files from the pinned npm
artifacts listed in `../ALPINE_067_SUPPLY.json`. They are served same-origin,
loaded only from the immutable browser feature plan, and are never fetched from
a CDN at runtime.

All listed Alpine packages are MIT licensed. The upstream license and source
references are recorded in the Phase 0.67 supply manifest and notice packet.
