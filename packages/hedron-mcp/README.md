# hedron-mcp

Experimental Alpha deny-by-default MCP Streamable HTTP projection for Hedron
(RFC-0043). Disabled and empty until resources and tools are explicitly
registered. MCP never grants authority beyond the authenticated principal.

```bash
pip install hedron-mcp
```

Enabling the package without registrations remains a no-op empty server.
