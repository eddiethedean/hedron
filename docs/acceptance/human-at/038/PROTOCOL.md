# Human AT protocol — phase 0.38 (scoped)

Representative keyboard and screen-reader sessions for the first-party `hedron-chart` element
and its JS-off figure/summary/table fallback.

This packet is **scoped AT evidence** for `A11Y-038` only. It does not claim Supported human AT
(see [#86](https://github.com/eddiethedean/hedron/issues/86) / D-052). Do not block 0.38 on
`SR-021`. Three-engine automated a11y remains required.

## Sessions (planned)

1. Inspect a beginner line chart with keyboard-only navigation after upgrade; confirm hover is
   not the only path.
2. Filter a series from the legend and reset zoom; confirm announcements stay bounded.
3. Complete the same inspect/filter path on the JS-off summary + bounded table fallback.

Record outcomes in `ledger/` using the redacted schema from `../ledger.schema.json`.
No sessions are recorded in this Stage 0 refine.
