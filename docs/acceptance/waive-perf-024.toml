# PERF-024 waive / supersede ledger (Disposition B / polling_only).
# Cut Accepted: polling_only — prior SSE/WS load Deferred ID superseded.

disposition_gate = "PERF-024"
owned_prior_ids = ["PERF-10-001"]
# Relative to repo root; used when disposition = prove_ops.
evidence_path = ""

[[rows]]
id = "PERF-10-001"
terminal = "superseded"
note = "Superseded by DECIDE-024 polling_only; load/proxy backpressure evidence not Required for Supported polling production."
owner = "hedron"
