schema_version = 1
phase = "0.55"
state = "Verified"
baseline = "v0.54.0"
target = "v0.55.0"
owning_rfc = "RFC-0082"

[[adapter]]
name = "fastapi"
layout = "supported"
capabilities = "supported"
replay = "supported"
upload_streaming = "degraded"
csp_nonce = "supported"
notes = "Primary reference adapter; uploads are buffered via materialize_upload (bytes), not streamed"

[[adapter]]
name = "flask"
layout = "supported"
capabilities = "unsupported"
replay = "unsupported"
upload_streaming = "degraded"
csp_nonce = "degraded"
notes = "MasterDetail/layout portable; capability/idempotency action kwargs are FastAPI-only in 0.55; buffered multipart accepted"

[[adapter]]
name = "django"
layout = "supported"
capabilities = "unsupported"
replay = "unsupported"
upload_streaming = "degraded"
csp_nonce = "degraded"
notes = "MasterDetail/layout portable; capability/idempotency action kwargs are FastAPI-only in 0.55; buffered multipart accepted"
