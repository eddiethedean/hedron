diff --git a/CHANGELOG.md b/CHANGELOG.md
index b9c5904..47c2386 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,12 @@
 # Changelog

+## [Unreleased / Hedron 0.59.0] — 2026-08-22
+
+### Changed
+- Migrated the shared shell, typed controls, navigation, sizing, and pipeline presentation to
+  the Hedron 0.59 contract while preserving Data Mover routes, CSRF, HTMX behavior, and domain
+  execution ownership.
+
 All notable changes to Data Mover are documented in this file.

 The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
diff --git a/app/static/theme.css b/app/static/theme.css
index 2a019d1..ba92359 100644
--- a/app/static/theme.css
+++ b/app/static/theme.css
@@ -84,10 +84,37 @@ button, a { -webkit-tap-highlight-color: transparent; }
 :focus-visible { outline: 3px solid #f8cd68; outline-offset: 3px; }
 ::selection { color: white; background: rgba(133, 135, 255, 0.52); }

-.page-width { width: min(1360px, calc(100% - 48px)); margin-inline: auto; }
-
 .banner-inner { min-height: 32px; display: flex; align-items: center; gap: 9px; }
-.flag-mark { color: var(--amber); font-size: 0.78rem; }
+.hedron-environment-banner {
+  border: 1px solid rgba(166, 167, 255, 0.2);
+  border-radius: 14px;
+  background: linear-gradient(100deg, rgba(53, 63, 133, 0.78), rgba(27, 70, 84, 0.58));
+  box-shadow: 0 14px 32px rgba(7, 12, 28, 0.18), inset 0 1px 0 rgba(255, 255, 255, 0.08);
+}
+.hedron-environment-banner::before {
+  color: var(--amber);
+  content: attr(data-hedron-mark);
+  font-size: 0.78rem;
+}
+.hedron-app-shell-banner {
+  padding: 0;
+  border: 0;
+  background: transparent;
+}
+.hedron-nav-status {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+}
+.hedron-nav-status::before {
+  width: 8px;
+  height: 8px;
+  flex: 0 0 auto;
+  border-radius: 50%;
+  background: var(--teal);
+  box-shadow: 0 0 0 4px rgba(57, 214, 191, 0.11), 0 0 18px rgba(57, 214, 191, 0.45);
+  content: "";
+}

 .site-header {
   position: relative;
@@ -161,15 +188,8 @@ button, a { -webkit-tap-highlight-color: transparent; }
 .nav-label { margin: 4px 0 9px 11px; color: #74829e; font-size: 0.63rem; font-weight: 850; letter-spacing: 0.15em; text-transform: uppercase; }
 .nav-label-spaced { margin-top: 28px; }
 .nav-link { position: relative; display: flex; align-items: center; gap: 11px; margin-bottom: 4px; padding: 10px 11px; border: 1px solid transparent; border-radius: 10px; color: #aeb8cf; font-size: 0.82rem; font-weight: 700; text-decoration: none; transition: color 0.16s ease, background 0.16s ease, border-color 0.16s ease, transform 0.16s ease; }
-.nav-link span { width: 23px; height: 23px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 7px; color: #7f8da8; background: rgba(255, 255, 255, 0.035); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 0.6rem; }
 .nav-link:hover { color: white; background: rgba(133, 135, 255, 0.08); border-color: rgba(133, 135, 255, 0.16); text-decoration: none; transform: translateX(2px); }
 .nav-link.active { color: white; background: linear-gradient(115deg, rgba(133, 135, 255, 0.26), rgba(133, 135, 255, 0.11)); border-color: rgba(166, 167, 255, 0.28); box-shadow: 0 10px 26px rgba(57, 53, 150, 0.16), inset 0 1px 0 rgba(255, 255, 255, 0.08); }
-.nav-link.active span { color: #dedfff; border-color: rgba(166, 167, 255, 0.28); background: rgba(133, 135, 255, 0.17); }
-.nav-status { display: flex; align-items: flex-start; gap: 10px; margin-top: 30px; padding: 14px 10px 4px; border-top: 1px solid var(--line); color: #aeb8ce; }
-.nav-status strong, .nav-status small { display: block; }
-.nav-status strong { font-size: 0.69rem; }
-.nav-status small { margin-top: 2px; color: #74819b; font-size: 0.63rem; }
-.status-dot { display: inline-block; width: 8px; height: 8px; flex: 0 0 auto; margin-top: 4px; border-radius: 50%; background: var(--teal); box-shadow: 0 0 0 4px rgba(57, 214, 191, 0.11), 0 0 18px rgba(57, 214, 191, 0.45); }

 .auth-intro h1, .auth-card h1, .auth-card h2 { font-family: var(--font-display); }
 .eyebrow, .section-number { margin: 0 0 9px !important; color: var(--teal) !important; font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 0.65rem !important; font-weight: 850; letter-spacing: 0.15em; text-transform: uppercase; }
@@ -181,8 +201,7 @@ button, a { -webkit-tap-highlight-color: transparent; }
 .identity-panel > p { margin: 0; color: var(--muted); font-size: 0.78rem; word-break: break-word; }

 .field-full { grid-column: 1 / -1; }
-.button-small { min-height: 32px; padding: 6px 10px; font-size: 0.67rem; }
-.button-wide { width: 100%; }
+.hedron-button[data-hedron-size="sm"] { min-height: 32px; padding: 6px 10px; font-size: 0.67rem; }
 .auth-main { position: relative; flex: 1; overflow: hidden; background: radial-gradient(700px 540px at 10% 35%, rgba(105, 103, 232, 0.23), transparent 68%), radial-gradient(620px 460px at 86% 48%, rgba(57, 214, 191, 0.08), transparent 70%); }
 .auth-main::before, .auth-main::after { position: absolute; z-index: 0; border-radius: 50%; filter: blur(2px); pointer-events: none; content: ""; }
 .auth-main::before { width: 430px; height: 430px; top: -280px; right: 14%; border: 1px solid rgba(166, 167, 255, 0.12); box-shadow: 0 0 100px rgba(133, 135, 255, 0.08); }
@@ -257,10 +276,6 @@ button, a { -webkit-tap-highlight-color: transparent; }
 .pending-row strong { max-width: 205px; overflow: hidden; font-size: 0.82rem; text-overflow: ellipsis; white-space: nowrap; }
 .pending-row small { color: var(--muted); font-size: 0.75rem; }

-.filter-form { display: flex; align-items: end; flex-wrap: wrap; gap: 10px; margin-bottom: 18px; padding: 14px; border: 1px solid var(--line); border-radius: var(--radius-small); background: var(--paper-soft); }
-.filter-form > div, .filter-form > .hedron-form-field { flex: 1 1 170px; }
-.filter-form label { display: block; margin-bottom: 5px; color: var(--muted-strong); font-size: 0.78rem; font-weight: 750; }
-.filter-form input, .filter-form select { width: 100%; min-height: 39px; padding: 8px 10px; border: 1px solid var(--line-strong); border-radius: 9px; color: var(--ink); background: rgba(6, 11, 24, 0.54); }
 code { padding: 2px 6px; border: 1px solid var(--line); border-radius: 6px; color: #c9cbff; background: rgba(133, 135, 255, 0.07); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 0.66rem; }

 .site-footer { margin-top: auto; border-top: 1px solid var(--line); color: #8f9bb3; background: rgba(4, 8, 17, 0.72); font-size: 0.75rem; }
@@ -300,9 +315,10 @@ form.htmx-request .button-action::after,
 }

 @media (max-width: 900px) {
-  .side-nav { position: static; display: flex; gap: 5px; overflow-x: auto; padding: 8px; }
-  .side-nav .nav-label, .nav-status { display: none; }
-  .nav-link { flex: 0 0 auto; margin: 0; }
+  .side-nav { position: static; }
+  .side-nav .nav-label { display: none; }
+  .hedron-app-shell-nav-footer { display: none; }
+  .nav-link { margin: 0; }
   .nav-link:hover { transform: none; }
   .auth-layout { gap: 34px; min-height: auto; padding-block: 48px; }
   .auth-intro { text-align: center; }
@@ -315,7 +331,6 @@ form.htmx-request .button-action::after,
 }

 @media (max-width: 660px) {
-  .page-width { width: min(100% - 24px, 1220px); }
   .header-inner { min-height: 72px; }
   .account-copy { display: none; }
   .brand small { display: none; }
@@ -348,6 +363,14 @@ form.htmx-request .button-action::after,
   .site-header, .side-nav, .hedron-card, .auth-card, .trust-list > div { background-color: #111a31; }
 }

+/* ---- Authenticated Hedron shell polish ---- */
+body:has(.hedron-app-shell) {
+  padding: 16px 24px 24px;
+}
+body:has(.hedron-app-shell) > .hedron-app-shell {
+  margin-top: 0;
+}
+
 /* ---- Data Mover data-movement workspace ---- */
 .pipeline-builder { padding: 27px; overflow: visible; }
 .pipeline-form { display: grid; gap: 25px; }
@@ -432,6 +455,7 @@ form.htmx-request .button-action::after,
 .provider-logo-target { color: #aef2e8; border-color: rgba(57, 214, 191, 0.23); background: linear-gradient(145deg, rgba(57, 214, 191, 0.18), rgba(133, 135, 255, 0.09)); }
 .node-kicker { margin: 0 !important; color: #75839e !important; font-size: 0.57rem !important; font-weight: 850; letter-spacing: 0.13em; text-transform: uppercase; }
 .provider-node .hedron-description-list dd { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+.provider-node .hedron-description-list dd > span { display: block; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: inherit; }

 .transfer-link { position: relative; z-index: 1; min-width: 0; text-align: center; }
 .transfer-track { position: relative; height: 44px; }
@@ -510,6 +534,7 @@ form.htmx-request .button-action::after,
   .run-history-list { grid-template-columns: 1fr 1fr; }
 }
 @media (max-width: 600px) {
+  body:has(.hedron-app-shell) { padding: 12px; }
   .pipeline-builder, .run-monitor, .run-history { padding: 20px; }
   .pipeline-section-heading { flex-direction: column; }
   .builder-actions { width: 100%; }
diff --git a/app/ui/forms.py b/app/ui/forms.py
index 588f1cd..a3dffc5 100644
--- a/app/ui/forms.py
+++ b/app/ui/forms.py
@@ -39,10 +39,6 @@ def submit_button(
         native_variant = "secondary"
     else:
         native_variant = "primary"
-    if wide:
-        classes.append("button-wide")
-    if small:
-        classes.append("button-small")
     existing = attrs.pop("class_", None)
     if existing:
         classes.append(str(existing))
@@ -51,6 +47,8 @@ def submit_button(
         class_=" ".join(classes) or None,
         type=cast(Literal["button", "submit", "reset"], type),
         variant=cast(Literal["primary", "secondary", "danger"], native_variant),
+        size="sm" if small else None,
+        width="full" if wide else None,
         **attrs,
     )
     return apply_action_recipe(button, variant=native_variant)
diff --git a/app/ui/layout.py b/app/ui/layout.py
index cfaec42..93114e1 100644
--- a/app/ui/layout.py
+++ b/app/ui/layout.py
@@ -10,9 +10,12 @@ from hedron import (
     AppShell,
     Badge,
     Container,
+    EnvironmentBanner,
     Fragment,
+    HtmxLink,
     Inline,
     Nav,
+    NavStatus,
     OobUpdate,
     Page,
     PageHeader,
@@ -26,7 +29,7 @@ from hedron_core.builtins import BusyRegion, RequestIndicator, SkipLink, SwapRev
 from app.config import Settings
 from app.dependencies import AuthContext
 from app.ui.forms import csrf_hidden, submit_button
-from app.ui.urls import asset_href, form_action, hx_attrs, page_href
+from app.ui.urls import asset_href, form_action, page_href

 INDICATOR = "#global-request-indicator"

@@ -108,23 +111,18 @@ def side_nav_children(request: Request, auth: AuthContext) -> list[NodeLike]:
         active = (
             "active" if normalized == href_norm or normalized.startswith(f"{href_norm}/") else ""
         )
-        return html.a(
-            html.span(number, aria={"hidden": "true"}),
-            f" {label}",
-            class_=f"nav-link {active}".strip(),
-            href=page_href(request, href),
-            **hx_attrs(
-                request,
-                method="get",
-                path=href.lstrip("/"),
-                target="#main-panel",
-                swap="outerHTML",
-                push_url=True,
-                select="#main-panel",
-                disabled_elt="#side-nav a.nav-link",
-                indicator=INDICATOR,
-                hx_ext="preload",
-            ),
+        return HtmxLink(
+            f"{number} · {label}",
+            page_href(request, href),
+            target="#main-panel",
+            swap="outerHTML",
+            push_url=True,
+            select="#main-panel",
+            disabled_elt="#side-nav",
+            indicator=INDICATOR,
+            preload="mouseover",
+            active=bool(active),
+            class_="nav-link",
         )

     children: list[NodeLike] = [
@@ -137,13 +135,6 @@ def side_nav_children(request: Request, auth: AuthContext) -> list[NodeLike]:
         children.append(html.p("Administration", class_="nav-label nav-label-spaced"))
         children.append(link("/admin/users", "04", "Team"))
         children.append(link("/admin/audit", "05", "Activity"))
-    children.append(
-        html.div(
-            html.span(class_="status-dot", aria={"hidden": "true"}),
-            html.div(html.strong("Sandbox healthy"), html.small("Credentials encrypted")),
-            class_="nav-status",
-        )
-    )
     return children


@@ -157,6 +148,15 @@ def side_nav(request: Request, auth: AuthContext) -> Nav:
     )


+def shell_nav_footer() -> NavStatus:
+    """Use Hedron's typed AppShell status slot for workspace health."""
+    return NavStatus(
+        "Sandbox healthy · Credentials encrypted",
+        tone="success",
+        mark="●",
+    )
+
+
 def side_nav_oob(request: Request, auth: AuthContext) -> OobUpdate:
     """Replace side-nav contents after in-shell navigation (preserves outer nav element)."""
     return OobUpdate(
@@ -212,29 +212,33 @@ def app_shell(
             nav=side_nav(request, auth),
             body=main_panel(*body),
             panel_id="main-content",
-            banner=Container(
-                html.span("▦", class_="flag-mark", aria={"hidden": "true"}),
-                html.span("Controlled workspace · Transfers are simulated in demo mode"),
-                class_="page-width banner-inner",
+            banner=EnvironmentBanner(
+                "Controlled workspace · Transfers are simulated in demo mode",
+                tone="info",
+                mark="▦",
             ),
             brand=brand,
             env_badge=environment_badge,
             account=(account_summary(request, auth, csrf_token=csrf_token) if csrf_token else None),
+            nav_footer=shell_nav_footer(),
             app_footer=Container(
                 html.span(settings.app_name),
                 html.span("Demo environment · No remote systems are contacted"),
-                class_="page-width footer-inner",
+                class_="footer-inner",
             ),
             content_width="wide",
         )
     else:
         banner = Container(
-            html.span("▦", class_="flag-mark", aria={"hidden": "true"}),
-            html.span("Controlled workspace · Transfers are simulated in demo mode"),
-            class_="page-width banner-inner",
+            EnvironmentBanner(
+                "Controlled workspace · Transfers are simulated in demo mode",
+                tone="info",
+                mark="▦",
+            ),
+            class_="banner-inner",
         )
         header = html.header(
-            Container(brand, environment_badge, class_="page-width header-inner"),
+            Container(brand, environment_badge, class_="header-inner"),
             class_="site-header",
         )
         content = html.main(*body, id="main-content", class_="auth-main", tabindex="-1")
@@ -242,7 +246,7 @@ def app_shell(
             Container(
                 html.span(settings.app_name),
                 html.span("Demo environment · No remote systems are contacted"),
-                class_="page-width footer-inner",
+                class_="footer-inner",
             ),
             class_="site-footer",
         )
diff --git a/app/ui/partials/admin_audit.py b/app/ui/partials/admin_audit.py
index 2d51716..fb7d0f0 100644
--- a/app/ui/partials/admin_audit.py
+++ b/app/ui/partials/admin_audit.py
@@ -13,6 +13,7 @@ from hedron import (
     ErrorState,
     Form,
     FormField,
+    FormGrid,
     Lazy,
     Loading,
     Section,
@@ -64,7 +65,8 @@ def audit_refresh_button(
         html.button(
             "Refresh",
             type="button",
-            class_="hedron-button hedron-button-secondary button-small",
+            class_="hedron-button hedron-button-secondary",
+            data={"hedron-size": "sm"},
             **hx_attrs(
                 request,
                 path=path,
@@ -147,49 +149,56 @@ def audit_results(

 def _audit_filter_form(request: Request, event_type_filter: str, outcome_filter: str) -> Form:
     return Form(
-        FormField(
-            name="event_type",
-            label="Event type",
-            id="event-type-filter",
-            control=TextInput(
-                "event_type",
+        FormGrid(
+            FormField(
+                name="event_type",
+                label="Event type",
                 id="event-type-filter",
-                value=event_type_filter,
-                placeholder="auth.login",
-                autocomplete="off",
+                control=TextInput(
+                    "event_type",
+                    id="event-type-filter",
+                    value=event_type_filter,
+                    placeholder="auth.login",
+                    autocomplete="off",
+                ),
             ),
-        ),
-        FormField(
-            name="outcome",
-            label="Outcome",
-            id="outcome-filter",
-            control=TextInput(
-                "outcome",
+            FormField(
+                name="outcome",
+                label="Outcome",
                 id="outcome-filter",
-                value=outcome_filter,
-                placeholder="success",
-                autocomplete="off",
+                control=TextInput(
+                    "outcome",
+                    id="outcome-filter",
+                    value=outcome_filter,
+                    placeholder="success",
+                    autocomplete="off",
+                ),
             ),
+            columns={"base": 1, "md": 3},
+            gap="sm",
         ),
-        submit_button("Apply filters", variant="secondary", small=True),
-        (
-            html.a(
-                "Clear",
-                class_="hedron-button hedron-button-secondary button-small",
-                href=page_href(request, "admin/audit"),
-                **hx_attrs(
-                    request,
-                    method="get",
-                    path="admin/audit",
-                    target="#audit-results-region",
-                    push_url=True,
-                    indicator=INDICATOR,
-                ),
-            )
-            if event_type_filter or outcome_filter
-            else html.div()
+        ActionGroup(
+            submit_button("Apply filters", variant="secondary", small=True),
+            (
+                html.a(
+                    "Clear",
+                    class_="hedron-button hedron-button-secondary",
+                    data={"hedron-size": "sm"},
+                    href=page_href(request, "admin/audit"),
+                    **hx_attrs(
+                        request,
+                        method="get",
+                        path="admin/audit",
+                        target="#audit-results-region",
+                        push_url=True,
+                        indicator=INDICATOR,
+                    ),
+                )
+                if event_type_filter or outcome_filter
+                else html.div()
+            ),
+            align="end",
         ),
-        class_="filter-form",
         action=form_action(request, "admin/audit"),
         method="get",
         aria={"label": "Filter audit events"},
diff --git a/app/ui/partials/admin_users.py b/app/ui/partials/admin_users.py
index 2b88040..c8b5371 100644
--- a/app/ui/partials/admin_users.py
+++ b/app/ui/partials/admin_users.py
@@ -13,6 +13,7 @@ from hedron import (
     Dialog,
     Form,
     FormField,
+    FormGrid,
     Heading,
     Section,
     Select,
@@ -65,7 +66,7 @@ def user_table(
                         hidden_field("q", query),
                         hidden_field("status", status_filter),
                         hidden_field("page", str(page)),
-                        Button(label, class_="button-small button-action", type="submit"),
+                        Button(label, class_="button-action", size="sm", type="submit"),
                         action=form_action(request, f"admin/users/{user.id}/{action}"),
                         method="post",
                         **hx_attrs(
@@ -88,7 +89,8 @@ def user_table(
                 hidden_field("page", str(page)),
                 Button(
                     action_label,
-                    class_="button-small button-action",
+                    class_="button-action",
+                    size="sm",
                     type="submit",
                 ),
                 action=form_action(request, f"admin/users/{user.id}/toggle"),
@@ -105,9 +107,12 @@ def user_table(
                     html.div(
                         html.button(
                             action_label,
-                            class_="hedron-button hedron-button-secondary button-small",
+                            class_="hedron-button hedron-button-secondary",
                             type="button",
-                            data={"hedron-dialog-open": f"#{dialog_id}"},
+                            data={
+                                "hedron-dialog-open": f"#{dialog_id}",
+                                "hedron-size": "sm",
+                            },
                         ),
                         Dialog(
                             "Disable account",
@@ -216,31 +221,34 @@ def user_directory(
     return Section(
         alert_box(success, kind="success"),
         Form(
-            FormField(
-                name="q",
-                label="Search",
-                id="user-query",
-                control=TextInput(
-                    "q",
+            FormGrid(
+                FormField(
+                    name="q",
+                    label="Search",
                     id="user-query",
-                    value=query,
-                    placeholder="email or name",
-                    type="search",
+                    control=TextInput(
+                        "q",
+                        id="user-query",
+                        value=query,
+                        placeholder="email or name",
+                        type="search",
+                    ),
                 ),
-            ),
-            FormField(
-                name="status",
-                label="Status",
-                id="status-filter",
-                control=Select(
-                    "status",
-                    status_options,
+                FormField(
+                    name="status",
+                    label="Status",
                     id="status-filter",
-                    value=status_filter or None,
+                    control=Select(
+                        "status",
+                        status_options,
+                        id="status-filter",
+                        value=status_filter or None,
+                    ),
                 ),
+                columns={"base": 1, "md": 3},
+                gap="sm",
             ),
-            submit_button("Filter", variant="secondary", small=True),
-            class_="filter-form",
+            ActionGroup(submit_button("Filter", variant="secondary", small=True), align="end"),
             action=form_action(request, "admin/users"),
             method="get",
             aria={"label": "Filter users"},
@@ -296,9 +304,12 @@ def invitation_panel(
                 html.div(
                     html.button(
                         "Revoke",
-                        class_="hedron-button hedron-button-danger button-small",
+                        class_="hedron-button hedron-button-danger",
                         type="button",
-                        data={"hedron-dialog-open": f"#{dialog_id}"},
+                        data={
+                            "hedron-dialog-open": f"#{dialog_id}",
+                            "hedron-size": "sm",
+                        },
                     ),
                     Dialog(
                         "Revoke invitation",
@@ -381,7 +392,7 @@ def invitation_panel(
                     required=True,
                 ),
             ),
-            Button("Send invitation", class_="button-wide", type="submit"),
+            Button("Send invitation", width="full", type="submit"),
             action=form_action(request, "admin/invitations"),
             method="post",
             **hx_attrs(
diff --git a/app/ui/partials/profile.py b/app/ui/partials/profile.py
index 9b697e6..607c498 100644
--- a/app/ui/partials/profile.py
+++ b/app/ui/partials/profile.py
@@ -148,7 +148,7 @@ def profile_identity(request: Request, auth: AuthContext, *, oob: bool = False)
         ),
         LinkButton(
             "Manage connections",
-            class_="button-wide",
+            width="full",
             href=page_href(request, "/security"),
         ),
         **attrs,
diff --git a/app/ui/partials/security.py b/app/ui/partials/security.py
index 411317e..0db360e 100644
--- a/app/ui/partials/security.py
+++ b/app/ui/partials/security.py
@@ -219,15 +219,19 @@ def secret_slot(
                 html.div(
                     Button(
                         "Replace credentials" if configured else "Save connection",
-                        class_="button-small button-action",
+                        class_="button-action",
+                        size="sm",
                         type="submit",
                     ),
                     (
                         html.button(
                             "Delete connection",
-                            class_="hedron-button hedron-button-danger button-small",
+                            class_="hedron-button hedron-button-danger",
                             type="button",
-                            data={"hedron-dialog-open": f"#delete-secret-{provider.name}"},
+                            data={
+                                "hedron-dialog-open": f"#delete-secret-{provider.name}",
+                                "hedron-size": "sm",
+                            },
                         )
                         if configured
                         else None
@@ -327,7 +331,8 @@ def connection_status_list(
                     Button(
                         "Test connection",
                         variant="secondary",
-                        class_="button-small button-action",
+                        class_="button-action",
+                        size="sm",
                         type="submit",
                     ),
                     action=form_action(request, f"security/secrets/{provider.name}/test"),
@@ -393,9 +398,12 @@ def session_list(
             action_node = html.div(
                 html.button(
                     "Revoke",
-                    class_="hedron-button hedron-button-danger button-small",
+                    class_="hedron-button hedron-button-danger",
                     type="button",
-                    data={"hedron-dialog-open": f"#{dialog_id}"},
+                    data={
+                        "hedron-dialog-open": f"#{dialog_id}",
+                        "hedron-size": "sm",
+                    },
                 ),
                 Dialog(
                     "Revoke session",
@@ -405,6 +413,7 @@ def session_list(
                         Button(
                             "Revoke session",
                             variant="danger",
+                            size="sm",
                             type="submit",
                         ),
                         action=form_action(request, f"profile/sessions/{session.id}/revoke"),
@@ -543,7 +552,8 @@ def security_activity_refresh(request: Request) -> NodeLike:
         html.button(
             "Refresh",
             type="button",
-            class_="hedron-button hedron-button-secondary button-small",
+            class_="hedron-button hedron-button-secondary",
+            data={"hedron-size": "sm"},
             **hx_attrs(
                 request,
                 path="/profile/activity",
diff --git a/app/ui/routes/pipeline.py b/app/ui/routes/pipeline.py
index 259322a..f416315 100644
--- a/app/ui/routes/pipeline.py
+++ b/app/ui/routes/pipeline.py
@@ -664,8 +664,11 @@ def _saved_pipeline_cards(
                     html.button(
                         "Load",
                         type="button",
-                        class_="hedron-button hedron-button-secondary button-small",
-                        data=_saved_pipeline_data(pipeline),
+                        class_="hedron-button hedron-button-secondary",
+                        data={
+                            **_saved_pipeline_data(pipeline),
+                            "hedron-size": "sm",
+                        },
                         **hx_attrs(
                             request,
                             method="get",
@@ -688,8 +691,11 @@ def _saved_pipeline_cards(
                         html.button(
                             "Run now",
                             type="submit",
-                            class_="hedron-button hedron-button-secondary button-small",
-                            data=_saved_pipeline_data(pipeline, run=True),
+                            class_="hedron-button hedron-button-secondary",
+                            data={
+                                **_saved_pipeline_data(pipeline, run=True),
+                                "hedron-size": "sm",
+                            },
                             disabled=not runnable,
                             title=(
                                 "Reconnect this pipeline's source and destination before running it."
@@ -1030,7 +1036,7 @@ def _pipeline_body(
                         Button(
                             "Save pipeline",
                             variant="secondary",
-                            class_="button-small",
+                            size="sm",
                             type="submit",
                         ),
                         html.button(
diff --git a/pyproject.toml b/pyproject.toml
index 2127460..7da7244 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -40,8 +40,8 @@ dependencies = [
   "cryptography>=42.0.8",
   "email-validator>=2.2",
   "fastapi>=0.141.1",
-  "hedron>=0.58.1,<0.59",
-  "hedron-posit>=0.58.1,<0.59",
+  "hedron>=0.59.0,<0.60",
+  "hedron-posit>=0.59.0,<0.60",
   "httpx2>=2.9.1",
   "pyjwt>=2.8",
   "pydantic-settings>=2.2.1",
diff --git a/requirements.txt b/requirements.txt
index febcce9..3ddd0ff 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -4,8 +4,8 @@ alembic>=1.13.1
 cryptography>=42.0.8
 email-validator>=2.2
 fastapi>=0.141.1
-hedron>=0.58.1,<0.59
-hedron-posit>=0.58.1,<0.59
+hedron>=0.59.0,<0.60
+hedron-posit>=0.59.0,<0.60
 httpx2>=2.9.1
 pyjwt>=2.8
 pydantic-settings>=2.2.1
