# Architectural decisions

This is the authoritative decision log for the phase 0.0 specification baseline. Accepted decisions remain in force until superseded by a later numbered entry.

| ID | Status | Decision |
|---|---|---|
| D-001 | Accepted | Python is the reference implementation; pure Python remains supported. |
| D-002 | Accepted | `hedron` is the FastAPI flagship distribution; Flask and Django use separate adapters. |
| D-003 | Accepted | `HedronRoute`, `HedronRouter`, response classes, lifespan, middleware, DI, StaticFiles, and OpenAPI hooks are the integration foundation. |
| D-004 | Accepted | `hedron-core` imports no FastAPI, Flask, Django, ASGI, or WSGI types. |
| D-005 | Accepted | JSON endpoints return models; HTML endpoints return components. |
| D-006 | Accepted | `Hedron()` may be a convenience application, but it is a thin composition of FastAPI extension points, not a parallel runtime. |
| D-007 | Accepted | Components are renderable by default; addressability and public exposure are explicit. |
| D-008 | Accepted | HTMX is the default request/swap layer; Web Components own persistent browser-local interaction. |
| D-009 | Accepted | Hedron-owned `Model`, `Props`, `FormModel`, and `Field` expose a constrained Pydantic-backed type system. |
| D-010 | Superseded | HDN was selected as an optional advanced language; D-039 reopens whether Hedron should own a separate declarative language at all. |
| D-011 | Accepted | Scoped styles use structural CSS rewriting, stable names, external assets, and light DOM by default. |
| D-012 | Accepted | The Component Explorer uses the same registry as rendering, routing, OpenAPI, tests, and assets. |
| D-013 | Accepted | Security is cross-cutting: contextual escaping, explicit trusted types, CSRF, safe URLs, private authenticated caching, and production-disabled Explorer. |
| D-014 | Accepted | Async I/O is supported at endpoints, dependencies, actions, sources, and plugin lifecycles; rendering stays synchronous until evidence requires otherwise. |
| D-015 | Accepted | The required dependency set stays small; integrations are lazy extras or separate packages. |
| D-016 | Accepted | DataEditor uses typed change sets and a Web Component; Tabulator is the initial default adapter. |
| D-017 | Accepted | `Auto()` uses an ordered, inspectable renderer registry and always permits explicit override. |
| D-018 | Superseded | Rust acceleration and cross-language bindings are deferred until contracts stabilize and benchmarks justify them. Superseded by D-048 for phase 0.14. |
| D-019 | Accepted | General streamed HTML is deferred; lazy addressable components are the default deferred-loading pattern. |
| D-020 | Accepted | Durable work belongs to external job systems; FastAPI `BackgroundTasks` is used only for small post-response work. |
| D-021 | Accepted | Hedron exposes scoped cache decorators and Streamlit-inspired utility components while leaving cache services, storage, and durable state to the application ecosystem. |
| D-022 | Superseded | The original pre-1.0 roadmap used cumulative releases 0.1 through 0.9; D-031 replaces only that numbering. |
| D-023 | Superseded | The initial matrix was CPython 3.12–3.14 with FastAPI `>=0.141.1,<0.142`, Pydantic `>=2.13.4,<2.14`, and bundled HTMX 2.0.10. Python range superseded by D-034. |
| D-034 | Accepted | Supported CPython is 3.11–3.14 (`requires-python = ">=3.11,<3.15"`). FastAPI `>=0.141.1,<0.142`, Pydantic `>=2.13.4,<2.14`, and bundled HTMX 2.0.10 remain as under the compatibility policy. |
| D-024 | Accepted | Hedron uses the documented monorepo distribution/import layout; Explorer is installed through `hedron[dev]`, not required by production `hedron`. |
| D-025 | Accepted | `@app.page`/`@router.page`, `@app.component`/`@router.component`, and `@app.action`/`@router.action` are the canonical app-local decorators. `@addressable` defines a reusable component resource that becomes reachable only through `include_component`. |
| D-026 | Accepted | Public code renders through opaque `ComponentNode`/`NodeLike` contracts and `render(...) -> RenderResult`; concrete serializer nodes and the serializer implementation remain private in 0.x. |
| D-027 | Accepted | Logical IDs are readable namespaced identifiers; generated instance and asset IDs use versioned SHA-256-derived formats with collision checks as specified in `IDENTIFIERS.md`. |
| D-028 | Accepted | The phase 0.1/0.2 built-in component catalog is fixed by `api/BUILT_INS.md`; later data, utility, and chart catalogs remain assigned to their roadmap phases. |
| D-029 | Accepted | The repository uses a uv workspace, Hatchling, Ruff, Pyright, pytest/pytest-anyio, and the configuration and diagnostic contracts documented for 0.0. |
| D-030 | Superseded | No open-source license is inferred; license selection blocks public publication but not local phase 0.1 implementation. Superseded by D-033. |
| D-031 | Superseded | The specification phase is 0.0 and the former stable target was phase 1.0. D-038 removes that target. |
| D-032 | Superseded | Established the original phase-to-release mapping through `v1.0.0`. D-038 retains `0.N` → `v0.N.0` but removes the scheduled 1.0 terminus. |
| D-033 | Accepted | Hedron is licensed under the MIT License; the repository root and each publishable distribution ship `LICENSE` with matching package metadata. |
| D-035 | Accepted | Phase 0.7 retains the `v0.7.0` train but is governed by a phase 0.6 behavioral closure gate and staged internal gates: portable adapter foundation, FastAPI operations, Flask, Django, jobs/conformance, and an optional-transport decision. Portable semantics are shared; ASGI, WSGI, and framework-native differences are explicit capability claims. Completion from phase 0.6 onward requires linked automated or immutable evidence; checked prose alone is not release evidence. Its former 1.0 release-train clause is superseded by D-038. |
| D-036 | Superseded | Django QuerySet as a first-party `hedron-data` DataSource is Deferred for the `v0.7.0` Supported adapter claim. Apps may bridge QuerySets themselves; Hedron does not advertise QuerySet paging/security as a portable contract until a later decision. Superseded by D-046 for phase 0.11. |
| D-046 | Accepted | Phase 0.11 ships a first-party bounded Django QuerySet `DataSource` in `hedron-data`. Applications supply an already-authorized base QuerySet; Hedron never discovers models or calls `.objects.all()` from user input. Ordering, filtering, projection, search, and page size are allowlisted; tenant/auth scoping is applied before client refinements and cannot be removed. Evaluation is forbidden at import, startup, diagnostics, and schema description. Query budgets, transaction ownership, and sync/async evaluation rules are explicit capability contracts. Flask Blueprint/`init_app`, Django `AppConfig`, Django forms bridging, portable adapter test harnesses, HDJ dynamic manifests/CSP reconciliation, Celery/RQ `JobBackend` bridges, and capability-labeled Flask/Django live helpers are in scope for `v0.11.0` as specified by ROADMAP §0.11 and D-044. |
| D-037 | Accepted | Phase 0.7 external cache and durable job conformance targets Redis (`redis` client, JSON `h1:` keys). FastAPI `BackgroundTasks` remains non-durable (D-020). Official HTMX SSE was Deferred through 0.9; phase 0.10 ships pinned `htmx-ext-sse` as **experimental** live observation while bounded polling remains the required Supported fallback (RFC-0032 / JOB-006; see What’s ready). |
| D-044 | Accepted | Phase 0.10 live transports follow RFC-0032: official SSE, focused streaming primitives only (D-019), page/session WebSocket channels for declared regions/client reads with HTTP/SSE/polling fallbacks, and opt-in navigation preload. Live/preload never becomes a hidden correctness dependency. FastAPI is the Supported host; Flask/Django live depth remains 0.11. |
| D-045 | Accepted | Phase 0.10 ships `Dialog`, `ChatMessage`, and `ChatInput` plus timed media chunk **transport** contracts. Chat history and model-provider state remain application-owned. First-party camera/microphone/Audio/Video capture UI remains phase 0.15. |
| D-038 | Accepted | Hedron has no scheduled 1.0 phase or arbitrary API-freeze terminus. Capability phases continue on the `0.x` line (`0.N` → `v0.N.0`), with stability and compatibility promises attached to documented contracts rather than inferred from a major version. The former deferred backlog is assigned through phases 0.9–0.14; D-041 subsequently swaps the 0.9 and 0.11 authoring/native-framework priorities. Every phase retains evidence, migration, and published-artifact gates. This supersedes D-031/D-032 and the 1.0/freeze portions of D-035. |
| D-039 | Superseded | Placed the experimental HDN language/runtime on a ground-up design hold; D-040 selects the replacement and retains the HDN deprecation boundary. |
| D-040 | Superseded | Selected optional Jinja authoring and a staged HDN removal; D-041 keeps the replacement but removes the compatibility schedule. |
| D-041 | Accepted | Phase 0.9 replaces HDN immediately with the separate optional `hedron-jinja` distribution. Python components remain canonical; Jinja templates are trusted application code with explicit component bindings and strict defaults. HDN source discovery, parser/evaluator/formatter, render program, artifacts, public APIs, CLI/Explorer paths, examples, and tests are removed with no compatibility flag, converter, or legacy package. Version 0.8 is the final HDN-capable line. Block component syntax uses explicit `with body` to avoid an ambiguous Jinja grammar. |
| D-042 | Superseded | Defined standards-first HDJ without a distinct file format; D-043 retains the standards-first boundary but makes `.hdj` an explicit versioned format. |
| D-043 | Accepted | `.hdj` is the canonical Hedron Jinja template format. A mandatory, static TOML prologue declares format version, template kind, a named feature profile, additional feature IDs, required deployment capabilities, assets, and fragment regions; the remaining body is ordinary Jinja/HTML. Feature declarations improve tooling and production inventory but never authorize routes/actions or weaken SecurityPolicy/CSP. HDJ loaders accept `.hdj` sources; ordinary Jinja files remain usable outside HDJ or through an explicitly declared foreign-template boundary. |
| D-047 | Accepted | Phase 0.12 owns data and visualization scale: advanced DataEditor (formulas, merges, pivots, trees, collaborative editing, spreadsheet I/O beyond CSV), shared column catalog with display-versus-write policy, typed grid/chart events and saved views, explicit `TransformPlan` with Explorer visibility, Dask/Snowflake bounded sources, beginner Area/Bar/Scatter charts, Plotly typed events, annotation overlays, and the named optional visualization adapters (Vega-Lite, PyDeck, MapLibre, Folium, GraphViz, Mermaid, Chart.js, Great Tables, Sigma/NetworkX, Three.js, ECharts, Datashader, Bokeh, HoloViews/hvPlot, Pygal, Plotly resampling). Optional adapters ship as first-party extras with local-asset/CSP/payload/lifecycle/accessible-fallback contracts. HDJ `hedron.data`/`hedron.charts` provider parity is in scope. Phase cut `v0.12.0` requires every `release-gate-0.12.toml` row Verified with zero Deferred, including three-engine browser and a11y matrices. AG Grid claims cover Community client and infinite row models only. |
| D-048 | Accepted | Phase 0.14 lifts D-018 under evidence gates: a published language-neutral conformance-test kit is the cross-language contract; experimental Java and Node runtimes and optional Rust acceleration may ship when they pass that kit. Pure Python remains the semantic reference and Supported fallback (D-001). Native acceleration requires named bottleneck, target improvement, benchmark corpus, parity suite, reproducible wheels/source-build, memory-safety/fuzz evidence, and material end-to-end benefit. Runtime or accelerator absence never changes public semantics, security policy, or deterministic output. HDJ exact loop/macro instrumentation, contracted extension evidence, scoped-style/validated-attribute helpers, broader contextual analysis, and portable checker fixtures close `HDJ-DEF-014` while preserving Jinja semantics and a pure-Python fallback. |
| D-049 | Accepted | Phase 0.18 ships model-demo/inference contracts on the Beta train (hedron-core/hedron 0.18.0): InferenceInterface/ModelDemo (fail-closed, registered actions only), ExampleSet/presentation/feedback, InferencePolicy over JobBackend, interaction recorder, and typed inference workflows with structured non-canvas editor. Optional hedron-gradio ships as independent Alpha 0.1.x with no core cost when absent. Deliberate non-parity: mutable globals, default-public UI APIs, raw JS injection, cwd file exposure, share tunnels, host-code editing. Feedback never silent or ground truth. In-process queues are development-only. |
| D-050 | Accepted | Phase 0.19 owns accessibility engineering and inclusive authoring on the Beta train: versioned WCAG 2.2 A/AA + WAI-ARIA 1.2 profile (`PROFILE-019`), machine-readable `AccessibilityContract` catalog, WCAG 2.2 interaction/media/cognitive/i18n packets, ATAG authoring assistance, Explorer accessibility workspace, `AccessibilityScenario`/ACT-axe evidence, evidence inventory/statement/waiver governance, and progressive-enhancement / landmark / Page-script work (`PE-019`, `LANDMARK-019`, `SCRIPT-019`). **`AT-019` for `v0.19.0`** is the automated three-engine Playwright keyboard/zoom/reduced-motion/forced-colors matrix plus pinned axe/ACT provenance after representative dynamic states — not compensated disabled-participant evaluation. Human screen-reader / compensated user evaluation is **Deferred → 0.21** (or a later maintenance packet) with an owning ROADMAP destination; it does not block the 0.19-owned zero-Deferred cut. Owning RFCs: RFC-0023 (umbrella) and RFC-0051–RFC-0055. Issues #8, #27, #31, and #39 remain normative for PE/landmark/script acceptance criteria. Hedron never automatically emits WCAG conformance, legal-compliance, certification, or ACR/VPAT claims. Cut `v0.19.0` requires every `release-gate-0.19.toml` row Verified with zero Deferred among 0.19-owned rows. |
| D-051 | Accepted | Phase **0.20** owns the production security floor and adapter-parity packet on the Beta train: HTMX browser hardening (`HTMX-020`), Python `html.*` `js:` eval reject (`EVAL-020`), trusted mount-path helpers (`MOUNT-020`), production startup security gates (`PROD-020`), Flask/Django `fragment_regions` parity (`REGION-020`), portable profile header/CSP application on adapters (`CSP-020`), Flask-Login `AuthSignal` verify/docs (`AUTH-020`), `hedron new --flask`/`--django` (`SCAFFOLD-020`), and adapter clean-wheel CI smoke (`WHEEL-020`), plus `REGRESS-020` / `PKG-020`. Zero Deferred among 0.20-owned rows at cut (`release-gate-0.20.toml`). `CSP-020` applies existing security profiles on Flask/Django without FastAPI imports; it does not invent per-header merge. Pluggable CSRF strategies, composable `SecurityPolicy` header merge/override, and `CsrfField` / Form HTMX kwargs (#36–#38) are **Deferred → 0.22** (Published `v0.22.0`; gates Verified) so 0.20 stays cuttable without a new CSRF protocol. **0.22 packet refined** (gate IDs `CSRF-022` / `HEADERS-022` / `FORM-022` / `REGRESS-022` / `PKG-022`; `release-gate-0.22.toml` / `RELEASE_0_22.md` / `api/CSRF_COMPOSITION.md` normative; issues remain linked for history) — split unchanged. Human AT remains **0.21** (D-050 / D-052). Owning RFCs: RFC-0012, RFC-0021, RFC-0028 (Accepted baselines; phase deltas). Issues #1, #3, #6, #12, #14, #17, #18, #19, and #20 remain normative for 0.20 acceptance criteria. Does not renumber earlier phases. |
| D-052 | Accepted | Phase **0.21** owns human assistive-technology and compensated disabled-participant evaluation deferred from `AT-019` / D-050. Automated Playwright/axe evidence remains `AT-019` on the 0.19 train and is not substituted by human AT. **Two cut levels:** (1) **Engineering publish** `v0.21.0` — protocol packet, PE corpus, fragment/DataEditor hardening, `PROTOCOL-021` / `REGRESS-021` / `PKG-021` Verified; session gates may remain Planned with `--allow-planned` and must not be marketed as Supported human AT. (2) **Verified human-AT cut** — VoiceOver + Safari (macOS); NVDA + Firefox (Windows); TalkBack + Chromium (Android); ≥2 compensated sessions with ≥1 screen-reader user and ≥1 other disability category (motor, low-vision, or cognitive); every 0.21-owned row Verified (zero Deferred) with `--require-sessions`. JAWS, iOS VoiceOver, NVDA+Chromium second pass, and voice/switch lab sessions are optional stretch evidence (not gate rows). Task corpus is `examples/reference-app` critical flows (login, CRUD form PE+HTMX, fragment status/refresh, DataEditor smoke). Gate IDs: `PROTOCOL-021`, `SR-021`, `PARTICIPANT-021`, `ARTIFACT-021`, `REMEDIATE-021`, `REGRESS-021`, `PKG-021` (`release-gate-0.21.toml`). Protocol and redacted ledger templates live under `docs/acceptance/human-at/`; raw consent notes and participant identifiers never enter git. Statements continue via human-approved `AccessibilityStatement` export (no auto WCAG/legal/VPAT/ACR). Owning RFC baseline: RFC-0055 (amended). Does not renumber earlier phases or absorb CSRF work (0.22 / D-051). |
| D-053 | Accepted | **Production-quality maturity program** (no calendar `1.0`). North star: raise **adopter trust for the Supported surface** — not “every package goes GA,” not commercial SLA/WCAG claims. Preserves D-038 (no scheduled 1.0). Priority stack: **P0** finish 0.21 human AT sessions + remediate, then (done) ship 0.22 CSRF/SecurityPolicy composition; **P1** expand the `stable` API tier for Supported CRUD/HTMX/jobs (phase **0.23**) and decide live-transport disposition — prove `BROWSER-10-001` / `PERF-10-001` / `LIVE-011-BROWSER` **or** formally document polling-only for production (phase **0.24**); **P2** reference-app production archetype + load budgets + extras landmine quarantine / charts graduation path (phase **0.25**); **P3** external security review + attach SBOM/evidence bundles on every train tag, and an optional written **`1.0` definition of done without a date** (stable tier covers Supported CRUD/admin + human AT Verified + live either Supported-with-proof or formally polling-only in production docs). Owning RFC: [RFC-0056](rfcs/RFC-0056-PRODUCTION-QUALITY.md). Adopter summary: [guides/production-quality.md](guides/production-quality.md). Does not renumber 0.21/0.22 or weaken release-gate honesty. |
| D-054 | Accepted | Phase **0.26** owns the first **production-grade package graduation** under the ROADMAP 0.26+ contract for `hedron-core`, `hedron`, and `hedron-explorer` on the documented server-rendered CRUD/admin Supported surface only. Baseline Published **`v0.25.2`**. Gate IDs: `CONTRACT-026`, `CORE-026`, `FASTAPI-026`, `EXPLORER-026`, `REVIEW-026`, `REGRESS-026`, `PKG-026` (`release-gate-0.26.toml`). Machine-readable inventory: `production-grade-inventory-026.toml`. Upgrade fixtures from `v0.25.2`. Independent security review (external firm or structured maintainer-led review with redacted report + disposition ledger; critical/high fixed). Does **not** promote SSE/WS/streaming/preload, make Explorer public-by-default, schedule `1.0`, or claim every Beta symbol is stable. Owning RFC: [RFC-0057](rfcs/RFC-0057-PRODUCTION-GRADE-CORE.md). Cut `v0.26.0` requires every 0.26-owned row Verified with zero Deferred. |
| D-055 | Accepted | Phase **0.27** owns **production-grade package graduation** under the ROADMAP 0.26+ contract for `hedron-data`, `hedron-flask`, `hedron-django`, `hedron-jinja`, and `hedron-extras` on explicitly bounded Supported workflows only. Baseline Published **`v0.26.0`**. Gate IDs: `DATA-027`, `FLASK-027`, `DJANGO-027`, `HDJ-027`, `EXTRAS-027`, `PARITY-027`, `REGRESS-027`, `PKG-027` (`release-gate-0.27.toml`). Machine-readable inventory: `production-grade-inventory-027.toml`. Upgrade fixtures from `v0.26.0`. Host-only install matrices; portable FastAPI/Flask/Django interaction/security parity; gate-owned adversarial suites plus structured maintainer review for satellite trust boundaries. Does **not** promote experimental live transports, graduate `experimental-ui` / CodeEditor host stub / specialty bridges, make Explorer audit durable (`REV-026-003`), graduate charts/native/MCP/Gradio, schedule `1.0`, or claim every Beta symbol is stable. Owning RFC: [RFC-0058](rfcs/RFC-0058-PRODUCTION-GRADE-SATELLITES.md). Cut `v0.27.0` requires every 0.27-owned row Verified with zero Deferred. |
| D-057 | Accepted | Phase **0.30** owns **production-grade `hedron-workbench`** under the ROADMAP 0.26+ contract for a Posit Workbench / RStudio Server deployment adapter on explicitly bounded Supported workflows only. Baseline Published **`v0.28.2`**. Gate IDs: `CONTRACT-029`, `RESOLVE-029`, `PATH-029`, `URL-029`, `RUNNER-029`, `DX-029`, `SECURITY-029`, `REALWB-029`, `COMPAT-029`, `PERF-029`, `REGRESS-029`, `PKG-029` (`release-gate-0.29.toml`). Machine-readable inventory: `production-grade-inventory-029.toml`. Upgrade fixtures from `v0.28.2`. Tracking [#134](https://github.com/eddiethedean/hedron/issues/134). Automatic path is `hedron-workbench run` with mount discovery and `HEDRON_ROOT_PATH` export **before** application import; native path is `HedronWorkbench` with inactive ordinary-Hedron parity and resolved launcher handoff; explicit generic-ASGI path is idempotent `workbenchify`; install/import/`RS_SERVER_URL` never auto-wrap or grant trust. Cookie Path remains construction-time via existing MOUNT-020. The built-in pre-bound runner rejects reload/multi-worker topologies; Posit Connect base-header remains Experimental. Flask/Django/WSGI, vendoring `fastapi-workbench`, bundling `rserver-url`, Connect publishing, and Workbench-auth-as-identity are excluded. Owning RFC: [RFC-0062](rfcs/RFC-0062-POSIT-WORKBENCH-ADAPTER.md). Cut `v0.30.0` requires every 0.29-owned row Verified with zero Deferred. |
| D-058 | Accepted | Phase **0.30** brings the existing PyPI `fastapi-workbench` project into this monorepo as `packages/fastapi-workbench` and makes its first repository-developed release **`1.0.0`** on an independent semantic-version line. Plain FastAPI applications receive the hands-off `fastapi-workbench run module:app` / factory experience without installing or importing Hedron. Generic Workbench discovery, typed configuration, ASGI HTTP/WebSocket normalization, pre-import launcher, redacted diagnostics, and shared Workbench fixtures belong to `fastapi-workbench`; it has no Hedron dependency. Coordinated `hedron-workbench` **`0.30.0`** declares `fastapi-workbench>=1.0.0,<2.0`, delegates that generic implementation, and retains only Hedron-specific facade, construction-time mount/cookie handoff, diagnostics, URL/assets/Explorer integration, and compatibility APIs. The public 0.3.4 line is the explicit upgrade source. Gate IDs: `CONTRACT-030`, `PACKAGE-030`, `RESOLVE-030`, `PATH-030`, `URL-030`, `FASTAPI-030`, `RUNNER-030`, `DX-030`, `DEPENDENCY-030`, `COMPAT-030`, `SECURITY-030`, `REALWB-030`, `PERF-030`, `REGRESS-030`, `PKG-030` (`release-gate-0.30.toml`). Machine-readable inventory: `production-grade-inventory-030.toml`. Tracking [#135](https://github.com/eddiethedean/hedron/issues/135). Owning RFC: [RFC-0063](rfcs/RFC-0063-FASTAPI-WORKBENCH-EXTRACTION.md). Cut `v0.30.0` / `fastapi-workbench` `1.0.0` requires every 0.30-owned row Verified with zero Deferred. `fastapi-workbench` 1.0.0 is not Hedron 1.0. |
| D-059 | Accepted | Phase **0.31** owns the dual **production-grade tooling** packet under the ROADMAP 0.26+ contract: (1) graduate `hedron-conformance`, `hedron-sample-kit`, `hedron-sim`, `hedron-notebook`, and the Node/Java conformance evaluators for stated tooling roles only ([RFC-0064](rfcs/RFC-0064-PRODUCTION-GRADE-TOOLING.md); [#87](https://github.com/eddiethedean/hedron/issues/87)); (2) ship the reviewable Streamlit AST migration assistant `hedron migrate streamlit` ([RFC-0061](rfcs/RFC-0061-STREAMLIT-AST-MIGRATOR.md); [#88](https://github.com/eddiethedean/hedron/issues/88)). Baseline Published **`v0.30.0`**. Gate IDs: `CONF-031`, `PLUGIN-031`, `SIM-031`, `NOTEBOOK-031`, `NODE-031`, `JAVA-031`, `MIGRATE-031`, `REGRESS-031`, `PKG-031` (`release-gate-0.31.toml`). Machine-readable inventory: `production-grade-inventory-031.toml`. Tooling-grade ≠ application production servers. Does **not** host multi-user notebooks, claim full HTMX/sim parity, treat sample-kit as a required runtime, promise Streamlit call-for-call parity, execute source apps by default, graduate MCP/Gradio, or schedule `1.0`. Historical note: D-057’s Accepted text still says “Cut `v0.30.0`” for the 0.29 Workbench gates; the Published Workbench adapter cut is **`v0.29.0`** and D-058 owns `v0.30.0` — D-059 does not rewrite D-057. Cut `v0.31.0` requires every 0.31-owned row Verified with zero Deferred. |
| D-060 | Accepted | Phase **0.32** owns **production-grade `hedron-mcp`** under the ROADMAP 0.26+ contract as a deny-by-default, authenticated MCP projection on an explicitly bounded Supported inventory only. Baseline Published **`v0.31.0`**. Gate IDs: `PROTOCOL-032`, `AUTHZ-032`, `BOUNDS-032`, `AUDIT-032`, `REVIEW-032`, `REGRESS-032`, `PKG-032` (`release-gate-0.32.toml`). Machine-readable inventory: `production-grade-inventory-032.toml`. Tracking [#89](https://github.com/eddiethedean/hedron/issues/89). Owning RFC: [RFC-0065](rfcs/RFC-0065-PRODUCTION-GRADE-MCP.md); Alpha product contract remains [RFC-0043](rfcs/RFC-0043-MCP-PROJECTION.md). Package version policy at cut: independent satellite **`hedron-mcp` `0.2.0` Beta** (pin `>=0.2.0,<0.3`); Alpha `0.1.x` is the upgrade source — not train-locked `0.32.0`, not `1.0.0`. Install/mount grant no ambient authority; mutations without full evidence and vendor-specific extensions remain Experimental. Does **not** act as an IdP/secrets broker/approval/tenant model, auto-project components/routes, substitute for Gradio MCP (moved to 0.34 by D-061), or schedule Hedron `1.0`/SLA/certification. Cut `v0.32.0` / `hedron-mcp` `0.2.0` requires every 0.32-owned row Verified with zero Deferred. |
| D-061 | Accepted | Phase **0.33** is assigned to the unified `hedron-posit` Workbench and Connect deployment adapter proposed by [RFC-0066](rfcs/RFC-0066-HEDRON-POSIT.md) and tracked by [#167](https://github.com/eddiethedean/hedron/issues/167). Scheduling does not accept the Draft RFC or authorize implementation before its licensed Connect contract probe, exact cut matrix, compatibility boundary, and bridge need are resolved. The former planned phases move down by one without scope changes: Gradio **0.34**, whole-fleet closure **0.35**, and the Web Component program **0.36–0.41**. Published phases through 0.31 and the owned MCP 0.32 packet remain unchanged. New 0.33 gate IDs are `CONTRACT-033`, `PACKAGE-033`, `PARITY-033`, `WORKBENCH-033`, `CONNECT-033`, `BRIDGE-033`, `PERF-033`, `REVIEW-033`, `DOCS-033`, `REGRESS-033`, and `PKG-033`; all later planned gate suffixes move with their owning phase. |
| D-062 | Accepted | Phase **0.34** owns **production-grade `hedron-gradio`** under the ROADMAP 0.26+ contract for explicitly declared remote Gradio endpoints and bounded Hugging Face Space client paths only. Baseline Published **`v0.33.0`**. Gate IDs: `CONTRACT-034`, `COMPAT-034`, `EGRESS-034`, `FILES-034`, `JOBS-034`, `VENDOR-034`, `REVIEW-034`, `DOCS-034`, `REGRESS-034`, `PKG-034` (`release-gate-0.34.toml`); optional cross-cutting `PRESENT-034` is **non-blocking** for Gradio cut. Machine-readable inventory: `production-grade-inventory-034.toml`. Tracking [#90](https://github.com/eddiethedean/hedron/issues/90). Owning RFC: [RFC-0067](rfcs/RFC-0067-PRODUCTION-GRADE-GRADIO.md); Alpha product contract remains [RFC-0049](rfcs/RFC-0049-GRADIO-ADAPTER.md). Package version policy at cut: independent satellite **`hedron-gradio` `0.2.0` Beta** (pin `>=0.2.0,<0.3`); Alpha `0.1.x` is the upgrade source. Does **not** embed Gradio UI runtime, allow arbitrary caller URLs, substitute for MCP Gradio auto-composition, or schedule Hedron `1.0`/SLA/certification. Cut `v0.34.0` / `hedron-gradio` `0.2.0` requires every 0.34-owned Gradio gate Verified with zero Deferred; `PRESENT-034` may remain Planned and defer default presentation switch to 0.35. |
| D-063 | Accepted | Phase **0.35** owns **whole-fleet production-grade closure** under the ROADMAP 0.26+ contract: every publishable `packages/*` (and published Node/Java runtime) distribution has an owned Supported scope **or** an explicit terminal disposition (`production_grade` / `incubator` / `fixture` / `eol`). Baseline Published **`v0.34.0`**. Gate IDs: `FLEET-035`, `SOLVER-035`, `COMPOSE-035`, `DOCS-035`, `SUPPLY-035`, `REGRESS-035`, `PKG-035` (`release-gate-0.35.toml`). Machine-readable inventory: `production-grade-inventory-035.toml`. Tracking [#91](https://github.com/eddiethedean/hedron/issues/91). Owning RFC: [RFC-0068](rfcs/RFC-0068-WHOLE-FLEET-CLOSURE.md) (Draft at refine; Accepted before cut). Deferred **`PRESENT-034`** folds into **`FLEET-035` + `DOCS-035`** (inventory + docs agree on default presentation status); do **not** invent `PRESENT-035`. Does **not** rename the cut to `1.0`, freeze all experimental APIs, claim every feature Supported, schedule commercial SLA/WCAG/VPAT, keep abandoned packages to pad the fleet, or reopen `polling_only` without a separately accepted evidence packet. Cut `v0.35.0` requires every 0.35-owned row Verified with zero Deferred. |
| D-068 | Accepted | Phase **0.40** owns **Web Component authoring, React migration matrix, and interoperability** under [RFC-0060](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md) (extends D-064–D-067): public author kit and element scaffold, plugin/HDJ/Explorer/theme metadata parity, portable conformance fixtures, optional `@hedron/elements` modules/TS types only, Experimental React-island bridge as docs/reference only (not shipped inside `hedron-elements`), and the phase-owned medium/low remediation packet. Stage 0 living baseline is Published **`v0.39.0`**. Cut target: Hedron **`v0.40.0`**. Gate IDs: `AUTHOR-040`, `PLUGIN-040`, `HDJ-040`, `THEME-040`, `EXPLORER-040`, `CONF-040`, `MIGRATE-040`, `SUPPLY-040`, `REGRESS-040`, `PKG-040` (`release-gate-0.40.toml`). Tracking [#95](https://github.com/eddiethedean/hedron/issues/95). Locked answers: [RFC-0060 Resolved questions (D-068)](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md#resolved-questions-d-068); catalogs in [REACT_MIGRATION_MATRIX_040.md](implementation/REACT_MIGRATION_MATRIX_040.md) and [WEB_COMPONENT_INTERACTION_CONTRACTS.md](implementation/WEB_COMPONENT_INTERACTION_CONTRACTS.md). Scaffold command: **`hedron new element`** (accepted name). React-island may remain **Experimental** when other gates are Verified; do not market Supported islands or universal React parity. Medium/low remediations owned by 0.40 (issue bodies remain normative; `REGRESS-040` Verified only when closed): #162, #203, #204, #219, #220, #222. Does **not** authorize draft transfer/composition (0.41), whole-platform graduation (0.42), required npm/bundler/TS/Lit for Python authors, arbitrary remote modules, private Shadow-DOM customization as Supported, or Stage 0 tip/runtime bumps. Stage 0 / contract refine changes contracts only; cut requires zero Deferred 0.40 rows (React-island Experimental honesty excepted per Resolved questions). |
| D-069 | Accepted | Phase **0.41** owns **typed browser composition, bounded draft transfer, navigation, redacted diagnostics, and per-element failure isolation** under [RFC-0060](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md) (extends D-064–D-068). Stage 0 living baseline is Published **`v0.40.0`**; cut target is coordinated Hedron / `hedron-elements` **`v0.41.0`**, without a Stage 0 runtime or version bump. Composition is allowlisted `CustomEvent` → registered action / `InteractionGraph` only; event details are untrusted, cycle/depth/target/payload/concurrency bounded, and every graph has an ordinary form/link or full-fragment fallback. Draft transfer is opt-in, same-origin `sessionStorage` only, namespaced by application + route family + element contract + schema + authenticated subject fingerprint, TTL/entry/aggregate bounded, single-consume, and cleared on logout, subject/authority change, successful submit, discard, expiry, incompatibility, or rollback. Only declared draft fields transfer; secrets, files/blobs, trusted HTML, capabilities, auth/CSRF material, server state, responses, and preference state never do. Navigation enhances ordinary same-origin links/forms: HTMX history remains canonical; fragment-only movement uses native history/scroll semantics; focus/title restoration is deterministic; preload and View Transitions are feature-detected, reduced-motion-aware accelerators only. Traces record IDs/timings/outcomes, never payloads or user content. Gate IDs: `COMPOSE-041`, `STATE-041`, `NAV-041`, `TRACE-041`, `FALLBACK-041`, `BROWSER-041`, `REGRESS-041`, `PKG-041` (`release-gate-0.41.toml`). Tracking [#96](https://github.com/eddiethedean/hedron/issues/96); its stale 0.37 suffixes are superseded by D-066/D-069. The locked 14-issue regression packet is #70, #74, #85, #98, #103, #106, #135, #149, #150, #185, #186, #200, #202, #207; issue bodies remain normative and all must close for `REGRESS-041`. Does **not** authorize a global store/client router/offline authority, cross-tab or durable draft sync, arbitrary selectors/callbacks, navigation interception outside declared same-origin scope, reopening polling-only, production-grade Web Component graduation (0.42), or Supported human-AT claims. Cut requires zero Deferred 0.41 rows. |
| D-070 | Accepted | Phase **0.42** owns **production-grade Web Component platform graduation** under [RFC-0060](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md) (extends D-064–D-069): a machine-readable Supported element inventory, compatibility/upgrade/rollback evidence, independent browser/security review, element human-AT honesty (`AT-042`), performance and supply evidence, and the locked 32-issue medium/low fleet remediation packet. Stage 0 living baseline is Published **`v0.41.0`**; cut target is coordinated Hedron / `hedron-elements` **`v0.42.0`**, without a Stage 0 runtime or version bump. At cut, `hedron-elements` becomes **Beta** and **production-grade for the declared Supported inventory only** (train-aligned `0.42.0`, pin `>=0.42.0,<0.43`); Stage 0 kept Alpha / incubator / `>=0.41.0,<0.42`. Locked Supported tags: `hedron-example`, `hedron-field-text`, `hedron-field-choice`, `hedron-field-file`, `hedron-disclosure`, `hedron-dialog`, `hedron-action-async`, `hedron-data-editor`; `hedron-chart` is cross-referenced from Published `hedron-charts` `0.2.x` (do not re-cut charts); no new tags in 0.42. Named Supported subsets of `ElementStateOwnership`, `InteractionState`, `OptimisticMutation`, and `GestureOverlayCatalog` appear in the inventory; remainder stays Experimental/excluded; none may disappear. React-island remains **Experimental** (0.40 removal ledger; not inside `hedron-elements`). Specialty UI, vendor chart adapters, live SSE/WS/streaming/preload, arbitrary remote modules, private Shadow-DOM customization, and required npm/bundler for Python apps stay Experimental/excluded. `@hedron/elements` remains optional modules/TS mirror only. Browser floor = Playwright Chromium/Firefox/WebKit from `tests/browser/test_browser_matrix.py`; exact build IDs at cut. `AT-042` is inventory honesty for element workflows (form, navigation, data-editor, chart, swap/failure, plus default-presentation public form/settings/table/dialog) on the D-052 matrix; it does **not** close [#86](https://github.com/eddiethedean/hedron/issues/86) / `SR-021` and must not market product-wide Supported human AT. `REVIEW-042` is structured maintainer-led review (external firm allowed) with zero unresolved critical/high. Default-presentation 0.33–0.42 destination is satisfied by `STABLE-042` / `AT-042` / `PERF-042` — do **not** invent `PRESENT-042`. Gate IDs: `STABLE-042`, `COMPAT-042`, `REVIEW-042`, `AT-042`, `PERF-042`, `SUPPLY-042`, `REGRESS-042`, `PKG-042` (`release-gate-0.42.toml`). Tracking [#97](https://github.com/eddiethedean/hedron/issues/97); its stale 0.38 title/body are superseded by D-066/D-070. Locked remediation packet (issue bodies remain normative; `REGRESS-042` Verified only when closed): #99, #100, #108, #136, #137, #138, #139, #140, #141, #145, #146, #147, #148, #151, #152, #156, #160, #174, #175, #177, #187, #205, #206, #208, #217, #218, #238, #242, #243, #245, #246, #249. Locked answers: [RFC-0060 Resolved questions (D-070)](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md#resolved-questions-d-070). Does **not** authorize a global store, SPA rewrite, wrapping every component as a custom element, reopening `polling_only`, closing `SR-021`, PyPI Production/Stable or `1.0`, or Stage 0 tip/runtime bumps. Stage 0 / contract refine changes contracts only; cut requires zero Deferred 0.42 rows. |
| D-071 | Accepted | Phase **0.43** owns the additive **refreshable-view, command-handle, and typed-update authoring model** under [RFC-0070](rfcs/RFC-0070-REFRESHABLE-VIEWS.md), extending RFC-0008/0009/0015/0019/0023/0024/0025/0039/0040/0044/0053. Baseline is Published **`v0.42.0`**; cut target is **`v0.43.0`** with no Stage 0 runtime/version bump. Locked beginner nouns are **view**, **command**, **refresh**, and **update**. Add `@app.refreshable` → callable `FragmentHandle`, `@app.command` → `ActionHandle`, deterministic generated internal paths/ids with explicit compatibility escape hatches, `BoundFragment`, accessible `FragmentHost`, bounded `refresh(...)` intents, and portable `Patch`/`PatchSet` compiled into the existing region/`InteractionResult`/OOB/security stack. The server output is canonical: missing client target may resolve to the handle, disagreement fails closed. Refresh intents rerun normal GET routes rather than invoking undocumented FastAPI DI internals; direct patches remain the one-response path. High-level patch targets are app-owned registered handles, not arbitrary selectors. Existing `Hedron.region`/`fragment`, `FragmentRegion`, `fragment_regions=`, `RefreshButton.for_region`, `swap*`, `retarget`, `OobUpdate`, `InteractionPolicy`, and `InteractionResult` remain supported and are **not deprecated** in 0.43; existing decorator return identity does not change. New symbols start Beta and require explicit inventory promotion. Gate IDs: `VIEW-043`, `COMMAND-043`, `UPDATE-043`, `SECURITY-043`, `A11Y-043`, `BROWSER-043`, `TOOLING-043`, `COMPAT-043`, `PERF-043`, `DOCS-043`, `REGRESS-043`, `PKG-043` (`release-gate-0.43.toml`). Tracking [#311](https://github.com/eddiethedean/hedron/issues/311). Does **not** authorize hidden reactive dependency tracking, a global store/SPA/hydration/VDOM runtime, generated-route security, inferred authz/transactions, required custom elements/Node/live transports, closing `SR-021`, changing package maturity, scheduling `1.0`, or Stage 0 version/published-claim changes. Cut requires every 0.43-owned row Verified with zero Deferred. |
| D-072 | Accepted | Phase **0.44** owns additive **type-driven authoring and schema-derived interactions** under [RFC-0071](rfcs/RFC-0071-TYPE-DRIVEN-AUTHORING.md), extending RFC-0003/0004/0008/0015/0016/0019/0023/0024/0039/0040/0044 and RFC-0070. Original Stage 0 baseline was Published **`v0.42.0`**; **D-076** rebases the living/planning baseline to Published in-tree **`v0.43.0`**. Verified in-tree **`v0.43.0`** remains the hard Stage 1 prerequisite and 0.44 cut baseline; target is **`v0.44.0`** with no Stage 0 runtime/version bump. Add explicit Pydantic `BaseModel` boundaries marked by `ViewParams`/`FormBody`, immutable `Annotated` metadata (`Sensitive`, `InstanceKey`, `Control`, `Refreshes`, `Updates`), generic `FragmentHandle[Params, Content]` / `BoundFragment[Content]` / `ActionHandle[Input, Result]` / `Patch[Content]`, a conservative supported-field native form generator with explicit overrides, declared-effect verification, complete discriminated typed-outcome mappings, optional `RefreshableView` and `CommandHandler` lifecycles, and one versioned/redacted `TypeSchema` consumed by runtime and tooling. `Fallback` and `Cache` are not markers (D-073 / D-076). Functions remain the primary beginner API; classes and models are opt-in. Annotations declare sources and allowed effects but never execute effects or infer dependencies. Pydantic owns shape validation; Hedron owns source, identity, redaction, presentation, route, effect, and interaction policy. Runtime inspection is for trusted imported apps; static CLI analysis must not import or evaluate target code. Existing 0.42/0.43 behavior and third-party annotations remain unchanged without a Hedron opt-in marker/class. New symbols begin Beta. Gate IDs: `MODEL-044`, `TYPING-044`, `FORM-044`, `EFFECT-044`, `CLASS-044`, `SCHEMA-044`, `SECURITY-044`, `A11Y-044`, `BROWSER-044`, `TOOLING-044`, `COMPAT-044`, `PERF-044`, `DOCS-044`, `REGRESS-044`, `PKG-044` (`release-gate-0.44.toml`). A tracking issue is required before Stage 1. Does **not** authorize inferred authn/authz/tenancy/business validation/transactions/idempotency/retry safety, hidden reactive dependency tracking, automatic arbitrary-schema form generation, auto-rendering arbitrary Pydantic returns, required class handlers/type-checker plugins/browser frameworks/Node/live transports, private Pydantic/FastAPI internals, closing `SR-021`, maturity/`1.0` changes, or planning-time version/published claims. Cut requires Verified 0.43 plus every 0.44-owned row Verified with zero Deferred. |
| D-067 | Accepted | Phase **0.39** owns **rich data and visualization element convergence plus `OptimisticMutation`** under [RFC-0060](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md) (extends D-064 / D-065 / D-066): DataTable/DataEditor ABI migration, typed optimistic revisions on bounded collection edits, consumption of the Published 0.38 `hedron-chart` contract without a parallel renderer, map/media/editor/specialty inventory with owned Experimental exceptions, worker/WASM/stream/origin cleanup bounds, large-scenario perf/a11y budgets, and the phase-owned medium/low remediation packet. Stage 0 living baseline is Published **`v0.38.0`**. Cut target: Hedron **`v0.39.0`**. Gate IDs: `DATA-039`, `OPTIMISTIC-039`, `CHARTLINK-039`, `RICH-039`, `WORKER-039`, `PERF-039`, `A11Y-039`, `REGRESS-039`, `PKG-039` (`release-gate-0.39.toml`). Tracking [#94](https://github.com/eddiethedean/hedron/issues/94). Locked answers: [RFC-0060 Resolved questions (D-067)](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md#resolved-questions-d-067); catalogs in [WEB_COMPONENT_INTERACTION_CONTRACTS.md](implementation/WEB_COMPONENT_INTERACTION_CONTRACTS.md) §3 and [RICH_SURFACE_039.md](implementation/RICH_SURFACE_039.md). First `OptimisticMutation` inventory is **bounded DataEditor/collection cell edits only**; auth, irreversible destruction, payments, secrets, file publication, and cross-tenant moves stay deny-by-default. Chart integration consumes Published `hedron-chart` / `ChartSpec` only. Specialty UI (`CodeEditor` / `TerminalView` / device capture) remains Experimental unless separately graduated. Human AT for `A11Y-039` is a **scoped** keyboard/AT packet (same honesty as `A11Y-038` / [#86](https://github.com/eddiethedean/hedron/issues/86)); do not market Supported human AT and do not block 0.39 on `SR-021`. Medium/low remediations owned by 0.39 (issue bodies remain normative; `REGRESS-039` Verified only when closed): #73, #84, #102, #104, #105, #107, #113, #115, #116, #117, #118, #119, #120, #121, #176, #188, #189, #190, #191, #192, #193, #194, #221, #240, #241, #247, #248. Does **not** authorize authoring/React bridge (0.40), draft transfer/composition (0.41), whole-platform graduation (0.42), unbounded attribute payloads, required canvas/Shadow DOM/workers/WASM/live transports, or Stage 0 tip/runtime bumps. Stage 0 / contract refine changes contracts only; cut requires zero Deferred 0.39 rows. |
| D-066 | Accepted | Phase **0.38** owns the **high-fidelity `hedron-charts` upgrade** under [RFC-0069](rfcs/RFC-0069-HIGH-FIDELITY-CHARTS.md): typed versioned `ChartSpec` / deterministic `ChartPlan`, an ABI-conforming **`hedron-chart`** Web Component, pinned modular D3 implementation, SVG/Canvas rendering, publication-quality scales/layout/themes, typed keyboard/pointer/touch interactions, accessible summary/table/AT workflows, deterministic export, performance/leak budgets, security review, migration, and supply evidence. Stage 0 living baseline is Published **`v0.37.0`**. Cut targets: Hedron **`v0.38.0`**, independent Beta **`hedron-charts` `0.2.0`**. Gate IDs: `GRAMMAR-038`, `RENDER-038`, `DESIGN-038`, `VISUAL-038`, `INTERACT-038`, `A11Y-038`, `PERF-038`, `EXPORT-038`, `SECURITY-038`, `COMPAT-038`, `DOCS-038`, `REGRESS-038`, `PKG-038` (`release-gate-0.38.toml`). Tracking [#251](https://github.com/eddiethedean/hedron/issues/251). Locked answers: [RFC-0069 Resolved questions (D-066)](rfcs/RFC-0069-HIGH-FIDELITY-CHARTS.md#resolved-questions-d-066); catalogs in [CHART_SPEC.md](implementation/CHART_SPEC.md). Matplotlib remains Supported; Plotly/Altair and other vendor adapters remain explicit Experimental paths unless separately graduated. No raw callbacks/expressions, remote runtimes/data, consumer Node build, client authority, blanket D3-gallery parity, or early whole-platform graduation. Human AT for `A11Y-038` is a **scoped** keyboard/AT packet (same honesty as `AT-037` / [#86](https://github.com/eddiethedean/hedron/issues/86)); do not market Supported human AT and do not block 0.38 on `SR-021`. Medium remediations owned by 0.38 (issue bodies remain normative; `REGRESS-038` Verified only when closed): #71/#72 → `RENDER-038`; #75/#81/#201/#239 → `SECURITY-038`; #82 → `A11Y-038`; #83 → `PERF-038`. The previously planned Web Component phases move without scope loss: rich surfaces/`OptimisticMutation` **0.39**, authoring/React interop **0.40**, composition/draft transfer/navigation **0.41**, production-grade platform **0.42**; #94–#97 move with those capabilities. Stage 0 / contract refine changes contracts only; cut requires zero Deferred 0.38 rows. |
| D-065 | Accepted | Phase **0.37** owns **form-associated elements, `InteractionState`, semantic interactive primitives, and `GestureOverlayCatalog`** under [RFC-0060](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md) (extends D-064): `ElementInternals` with tested native/light-DOM fallback, ordinary/HTMX form parity, constraint and server validation integration, locked primitive catalog, shared async state machine, and gesture/overlay contracts. Baseline Published **`v0.36.0`**. Gate IDs: `FORM-037`, `VALIDITY-037`, `PRIMITIVE-037`, `ACTIONSTATE-037`, `INTERACT-037`, `HTMX-037`, `AT-037`, `REGRESS-037`, `PKG-037` (`release-gate-0.37.toml`). Tracking [#93](https://github.com/eddiethedean/hedron/issues/93). Locked reference tags: **`hedron-field-text`**, **`hedron-field-choice`**, **`hedron-field-file`**, **`hedron-disclosure`**, **`hedron-dialog`**, **`hedron-action-async`**; **`hedron-example`** remains the non-form ABI reference and must not regress `ABI-036`…`BROWSER-036`. `hedron-elements` continues **Alpha** on the coordinated train (cut target **`0.38.0`**, pin `>=0.38.0,<0.39` at cut only); registry `form_contract` fields become required at 0.37+. Typed 0.36 `CustomEvent` schemas remain; **`InteractionState` runtime is 0.37-only** — no retroactive `ACTIONSTATE-037` on `hedron-example`. Primitive catalog: disclosure, dialog, tabs, menu/popover, selection, bounded upload; native platform APIs preferred. Living tip stays **`0.36.x`** until `v0.38.0` cut. Does **not** authorize high-fidelity charts (0.38), `OptimisticMutation` (0.39), React bridge (0.40), draft transfer (0.41), production-grade/`stable` graduation (0.42), `ElementInternals` runtime, gesture modules, or workspace/tip bump in Stage 0. Cut `v0.38.0` requires every 0.37-owned row Verified with zero Deferred. |
| D-064 | Accepted | Phase **0.36** owns the **Web Component ABI and lifecycle foundation** under [RFC-0060](rfcs/RFC-0060-WEB-COMPONENT-PLATFORM.md): versioned element registry, Alpha `hedron-elements`, `ElementStateOwnership`, SSR/HTMX lifecycle, CSP/Trusted Types, and three-engine browser proof for one reference light-DOM element. Baseline Published **`v0.35.0`**. Gate IDs: `ABI-036`, `ELEMENTS-036`, `LIFECYCLE-036`, `SSR-036`, `STATE-036`, `SECURITY-036`, `A11Y-036`, `BROWSER-036`, `PKG-036` (`release-gate-0.36.toml`). Tracking [#92](https://github.com/eddiethedean/hedron/issues/92). Locked defaults: reference tag **`hedron-example`** (controlled `status` + disposable local UI; not form-associated); `hedron-elements` Alpha **`0.36.0`** train-aligned (`>=0.36.0,<0.37`, depends on `hedron-core` only); browser floor = Playwright Chromium/Firefox/WebKit matrix from `tests/browser/test_browser_matrix.py`; `BROWSER-036` “100 elements” means **100 upgrade/swap cycle instances** of the reference element, not 100 tag types; registry `form_contract` is a **metadata stub** until 0.37; typed `CustomEvent` schemas only (no `InteractionState` until 0.37); draft **transfer** deferred to 0.41 by D-066; presentation binds via `SSR-036`/`A11Y-036` — **do not** invent `PRESENT-036`. Post-0.35 Alpha registration amends the fleet inventory at 0.36 cut without reopening `FLEET-035`. Before the cut, the living tip remained **`0.35.x`**. Does **not** authorize hydration/VDOM/synthetic events/global browser store/application Node build, form-associated controls, gestures, optimism, production-grade/`stable` promotion, or Stage 0 package bootstrap. Cut `v0.36.0` requires every 0.36-owned row Verified with zero Deferred. |
| D-056 | Accepted | Phase **0.28** owns **production-grade package graduation** under the ROADMAP 0.26+ contract for `hedron-charts` and `hedron-native` on explicitly bounded Supported workflows only. Baseline Published **`v0.27.0`**. Gate IDs: `CHARTS-028`, `INTERACTIVE-028`, `NATIVE-028`, `SUPPLY-028`, `REGRESS-028`, `PKG-028` (`release-gate-0.28.toml`). Machine-readable inventory: `production-grade-inventory-028.toml`. Upgrade fixtures from `v0.27.0`. Static/Matplotlib beginner charts graduate with a11y/CSP/browser/export/cleanup/payload evidence; Plotly/Altair and every optional visualization adapter remain **Experimental** and must be machine-labeled and absent from production defaults (`INTERACTIVE-028` passes without graduating them). Native graduation covers optional Rust `escape_text` / `escape_attr` wheels for the Supported CPython × OS matrix, source builds, fuzz/sanitizer/parity, measured serialize-stage benefit, and absence/import-failure/unsupported-platform/runtime-disable fallback without semantic drift. Does **not** declare all chart backends Supported, require native for correctness, load Supported chart runtimes from unpinned CDNs, claim microbenchmark-only perf, graduate MCP/Gradio/conformance tooling, schedule `1.0`, or claim full human AT for charts. Owning RFC: [RFC-0059](rfcs/RFC-0059-PRODUCTION-GRADE-CHARTS-NATIVE.md). Cut `v0.28.0` requires every 0.28-owned row Verified with zero Deferred. |

| D-073 | Accepted | **D-071/D-072 cross-phase refinement:** phase 0.43 owns and freezes the runtime foundation that 0.44 consumes: `FragmentHandle[Bind, Content]`, `ActionHandle[Input, Result]`, `BoundFragment[Content]`, and `Patch[Content]`; structural route binding with normal GET validation authoritative; explicit-field forms; dynamic/observed effects; one versioned base handle descriptor/fingerprint; and one binding-adapter extension seam. Phase 0.44 specializes those existing generic slots with opt-in Pydantic models, implements the adapter through documented FastAPI/Pydantic APIs, adds generated fields, declared effect constraints, typed outcomes, and function-equivalent handler classes, and attaches a fingerprint-bound `TypeSchema` extension. It may not change generic arity, replace base routing/identity/host/output/security authority, synthesize a parallel request/DI pipeline, or change unmodeled 0.43 behavior. `Fallback` and `Cache` are explicit route/handle/class configuration, not `Annotated` markers. This refines conflicting wording in D-071/D-072 without changing phase numbers, gate IDs, baselines, targets, or authorizing runtime/version changes; [RFC-0070](rfcs/RFC-0070-REFRESHABLE-VIEWS.md) and [RFC-0071](rfcs/RFC-0071-TYPE-DRIVEN-AUTHORING.md) carry the normative boundary. |
| D-074 | Accepted | Phase **0.45** owns **typed interaction ecosystem convergence** under [RFC-0072](rfcs/RFC-0072-TYPED-INTERACTION-ECOSYSTEM.md). Original Stage 0 baseline was Published **`v0.42.0`**; **D-077** rebases the living/planning baseline to Published in-tree **`v0.44.0`**. Verified in-tree **`v0.44.0`** remains the hard Stage 1 prerequisite and 0.45 cut baseline; target is **`v0.45.0`** with no Stage 0 runtime/version bump. Add one sealed read-only `InteractionCatalog`, deterministic redacted `InteractionManifest`, namespaced bounded `PackageProjection` protocol, explicit package dispositions, and shared host/Explorer/CLI/OpenAPI/Jinja/scenario/conformance/sim/notebook/deployment consumers. The authority chain is fixed: 0.43 base descriptor → optional 0.44 `TypeSchema` → 0.45 catalog index → package projection. Catalog/manifest/projections never route, validate, authorize, execute, expose, or override. MCP/Gradio exposure remains separate explicit deny-by-default policy. Every first-party package/runtime receives an evidence-backed `native_consumer`, `projection_adapter`, `compatibility_only`, or `not_applicable` disposition; compatibility-only is valid and no package must invent meaningless APIs. Gate IDs: `CATALOG-045`, `MANIFEST-045`, `PROJECTION-045`, `HOST-045`, `AUTHOR-045`, `SURFACE-045`, `REMOTE-045`, `TOOLING-045`, `PORTABLE-045`, `DEPLOY-045`, `SECURITY-045`, `A11Y-045`, `BROWSER-045`, `COMPAT-045`, `PERF-045`, `DOCS-045`, `REGRESS-045`, `PKG-045`. A tracking issue is required before Stage 1. Does **not** authorize a third runtime/schema authority, static target-code execution, automatic remote exposure, dependency inversion, package-native workflows (0.46), maturity promotion, `SR-021` closure, or `1.0`. Cut requires Verified 0.44 and zero Deferred 0.45 rows. |
| D-075 | Accepted | Phase **0.46** owns **package-native typed workflows** under [RFC-0073](rfcs/RFC-0073-PACKAGE-NATIVE-WORKFLOWS.md). Original Stage 0 baseline was Published **`v0.42.0`**; **D-079** rebases the living/planning baseline to Published in-tree **`v0.45.0`**. Verified in-tree **`v0.45.0`** remains the hard Stage 1 prerequisite and 0.46 cut baseline; target is **`v0.46.0`** with no Stage 0 runtime/version bump. Add immutable atomic `FeatureBundle` inclusion that compiles only to existing handles/components/scenarios/catalog projections; an opt-in `DataWorkspace` initial inventory of list/detail/create/edit over explicit authorized sources/policies; explicit typed chart selection/filter/drill-down/export command/effect bindings; Supported schema-aware element controls with native fallback; separate explicit `McpExposure` and Gradio `RemoteWorkflow` policies; and catalog-backed Explorer/Jinja/extras/notebook/sim/scenario/conformance experiences. No package gains a workflow executor. Applications retain live authz/tenancy/transaction/idempotency/retry/destructive/egress/file/output authority, and every generated surface has override/eject/rollback. Gate IDs: `BUNDLE-046`, `DATAFLOW-046`, `VISUAL-046`, `ELEMENT-046`, `REMOTE-046`, `WORKBENCH-046`, `SCENARIO-046`, `ADAPTER-046`, `SECURITY-046`, `A11Y-046`, `BROWSER-046`, `COMPAT-046`, `PERF-046`, `DOCS-046`, `REGRESS-046`, `PKG-046`. A tracking issue is required before Stage 1. Does **not** authorize universal CRUD, ORM/source discovery, inferred policy, hidden reactivity, automatic remote exposure, arbitrary schema controls, opaque visual programming, mandatory browser/Node/live runtimes, blanket package promotion, `SR-021` closure, or `1.0`. Cut requires Verified 0.45 and zero Deferred 0.46 rows. |
| D-076 | Accepted | **0.44 Stage 0 contract refine against Published in-tree `v0.43.0`.** Rebases D-072/RFC-0071 planning onto the real 0.43 seams without reopening D-072 goals, D-073 handle/descriptor/adapter authority, gate IDs, CLASS-044, TOOLING-044, or 0.45/0.46 catalog/workflow ownership. Living/planning baseline becomes Published in-tree **`v0.43.0`**; cut target stays **`v0.44.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.43 is enough predecessor evidence; do not block Stage 1 on PyPI/GitHub `#311` assets. Closed `Annotated` markers: `ViewParams`, `FormBody`, `Sensitive`, `InstanceKey`, `Control`, `Refreshes`, `Updates`. `Fallback`/`Cache` remain 0.43 decorator/handle/class config (`fallback=`, `CacheHint`). Consume shipped `FragmentHandle[BindT, ContentT]`, `ActionHandle[InputT, ResultT]`, `BoundFragment[ContentT]`, `Patch[ContentT]`, `BaseHandleDescriptor.extensions` namespace **`hedron.type`**, `BindingAdapter` / `StructuralBindingAdapter` / `BindingPlan` / `BoundValues` / `descriptor_fingerprint`. `ViewParams`/`FormBody` accept Pydantic v2 `BaseModel` including Hedron `Model`/`FormModel`; `Field` presentation stays valid; `Control` is generated-form presentation override only; `secret=True` implies `Sensitive`, `identity=True` implies `InstanceKey`. `AutoForm`, explicit `Form(action=handle, ...)`, and `@app.action` stay the universal path; `ActionHandle.form()` is additive for opted-in `FormBody`. `ViewParams.source` does not invent routes: path placeholders own path fields; remaining serializable fields use query. Field-shape/`Control.kind` lock: [type-form-inventory-044.toml](acceptance/type-form-inventory-044.toml). `TypeSchema` payload lock: [type-schema-044.toml](acceptance/type-schema-044.toml). Adapter honesty: [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml). `OutcomeMap(case(...), ...)` is the frozen builder spelling. Version matrix: Python 3.11–3.14, Pydantic v2 public APIs, pinned FastAPI/Starlette, stock mypy/pyright. Reserve `HED-TYPE-*` names in docs only. Locked answers: [RFC-0071 Resolved questions (D-076)](rfcs/RFC-0071-TYPE-DRIVEN-AUTHORING.md#resolved-questions-d-076). Stage 0 / contract refine changes contracts only; a tracking issue is still required before Stage 1. Does **not** split CLASS-044 or fleet projection into 0.45, implement RFC-0072/0073, add 0.44 runtime symbols, or change package versions. |
| D-077 | Accepted | **0.45 Stage 0 contract refine against Published in-tree `v0.44.0`.** Rebases D-074/RFC-0072 planning onto the real 0.43 descriptor and 0.44 `TypeSchema` seams without reopening D-074 catalog/manifest/projection authority, gate IDs, CLASS-044, SCHEMA-044, FORM-044, TOOLING-044, or 0.46 workflow ownership. Living/planning baseline becomes Published in-tree **`v0.44.0`**; cut target stays **`v0.45.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.44 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#318`/`#311` assets. Consume shipped `FragmentHandle[BindT, ContentT]`, `ActionHandle[InputT, ResultT]`, `BoundFragment[ContentT]`, `Patch[ContentT]`, `BaseHandleDescriptor` (`kind` `view`/`command`, `version=1`), `descriptor_fingerprint` (SHA-256 canonical JSON, first 32 hex chars; does **not** hash `effect` or `extensions`), `BindingAdapter` / `StructuralBindingAdapter` / `BindingPlan` / `BoundValues`, explicit `Form(action=handle, ...)`, `TypeSchema` under **`hedron.type`** (`schema_version=1`, `type_schema_from_descriptor()`, `TypeSchema.stable_fingerprint()`), closed markers `ViewParams`/`FormBody`/`Sensitive`/`InstanceKey`/`Control`/`Refreshes`/`Updates`, and `OutcomeMap(case(...), ...)`. Catalog `effect_state` copies `BaseHandleDescriptor.effect`; `TypeSchema.effect_knowledge` remains `dynamic`/`declared` only; observed is never a declaration. CLASS-044 class handlers keep the same view/command kinds. Portable catalog/manifest/projection values live in `hedron-core`; `Hedron.interactions`, compiler/seal/CLI/OpenAPI/scenarios live in `hedron`. Flask/Django/Jinja stay projection adapters stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) with no FastAPI DI or annotation evaluation. Entry lock: [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). Manifest lock: [manifest-format-045.toml](acceptance/manifest-format-045.toml). Host portable facts: [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Form kinds stay [type-form-inventory-044.toml](acceptance/type-form-inventory-044.toml). Type payload stays [type-schema-044.toml](acceptance/type-schema-044.toml). Reserve `HED-CATALOG-*` / `HED-PROJECTION-*` names in docs only. Locked answers: [RFC-0072 Resolved questions (D-077)](rfcs/RFC-0072-TYPED-INTERACTION-ECOSYSTEM.md#resolved-questions-d-077). Stage 0 / contract refine changes contracts only; a tracking issue is still required before Stage 1. Does **not** implement RFC-0072 runtime, RFC-0073, split CLASS-044 into 0.45, add 0.45 runtime symbols, or change package versions. | |

| D-078 | Accepted | Phase **0.47** owns **first-class maps and offline geospatial presentation** under [RFC-0074](rfcs/RFC-0074-FIRST-CLASS-MAPS.md). Original Stage 0 baseline was Published in-tree **`v0.45.0`**; **D-082** rebases the living/planning baseline to Published in-tree **`v0.46.0`**. Verified **`v0.46.0`** remains the hard Stage 1 prerequisite and cut baseline; targets are Hedron **`v0.47.0`** and independent Beta **`hedron-maps` `0.1.0`** with no Stage 0 runtime/version claim. Add closed immutable `MapSpec` / deterministic redacted `MapPlan`; distinct basemap/source/layer/style/view/policy/event models; a replaceable attributed OSM raster default; exact-origin custom XYZ/TileJSON/MVT sources; pinned local strict-CSP MapLibre enhancement; initial marker/GeoJSON/line/polygon/circle/raster layers; typed bounded feature/viewport events through ordinary commands; and tested static-image, PMTiles, bounded MBTiles, blank-map, and manifest-driven air-gapped paths. Existing `hedron.Map` remains the semantic fallback and compatibility surface. Applications retain provider terms/capacity, egress, credentials, authz, proxy, archive, privacy, and action authority. Gate IDs: `SPEC-047`, `PROVIDER-047`, `OFFLINE-047`, `RENDER-047`, `INTERACT-047`, `SECURITY-047`, `A11Y-047`, `BROWSER-047`, `PERF-047`, `ADAPTER-047`, `TOOLING-047`, `COMPAT-047`, `DOCS-047`, `REGRESS-047`, `PKG-047`. A tracking issue is required before Stage 1. Does **not** authorize arbitrary MapLibre callbacks/plugins/remote styles, automatic credentials/providers/licensing, a general tile server/GIS, WMS/WMTS/WFS, arbitrary CRS, Leaflet/OpenLayers/deck.gl/Cesium graduation, terrain/globe/3D/drawing/routing/geocoding, offline prefetch against public services, browser authority, removal of semantic alternatives, blanket maturity promotion, `SR-021` closure, or `1.0`. Cut requires Verified 0.46 and zero Deferred 0.47 rows. |
| D-079 | Accepted | **0.46 Stage 0 contract refine against Published in-tree `v0.45.0`.** Rebases D-075/RFC-0073 planning onto the real 0.43 descriptor, 0.44 `TypeSchema`, and 0.45 catalog/manifest/projection seams without reopening D-075 bundle/workspace/chart/element/remote authority, gate IDs, CLASS-044, SCHEMA-044, FORM-044, CATALOG-045, or 0.47 maps ownership. Living/planning baseline becomes Published in-tree **`v0.45.0`**; cut target stays **`v0.46.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.45 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#328`/`#318`/`#311` assets. Consume shipped `FragmentHandle[BindT, ContentT]`, `ActionHandle[InputT, ResultT]`, `BoundFragment[ContentT]`, `Patch[ContentT]`, `BaseHandleDescriptor`, `descriptor_fingerprint` (does **not** hash `effect` or `extensions`), `Hedron.include_component`, `Hedron.interactions`, `compile_interaction_catalog` / `seal_app_catalog` after `seal_registry`, `InteractionCatalog` / `CatalogEntry` / `PackageProjection` / `ProjectionProvider` in `hedron_core.catalog`, `TypeSchema` under **`hedron.type`**, `DataEditorSource` / `AsyncDataEditorSource`, `DataTable` / `DataQuery` / `write_policy`, `OptimisticMutation` risk class `collection_edit`, first-party `hedron-chart` kinds, `ActionHandle.form()`, native-canonical `Control.kind` inventory, `McpProjection.register_resource` / `register_tool`, `GradioClientAdapter` / `GradioRemoteConfig` / `GradioEndpoint`, and `AppScenario`. `FeatureBundle` is pre-seal atomic registration, not an executor and not `FeatureManifest` / Jinja `ProviderManifest`. `DataWorkspace` produces a `FeatureBundle`; policy type is `DataWorkspacePolicy`. `ChartInteraction` binds untrusted events to `ActionHandle` plus `Refreshes`/`Updates`. Portable bundle values live in `hedron-core`; `Hedron.include_feature` lives in `hedron`; `FeatureProvider` is a core Protocol not on the `hedron` facade. Flask/Django stay projection adapters stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) and [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Bundle lock: [feature-bundle-046.toml](acceptance/feature-bundle-046.toml). Workspace lock: [data-workspace-046.toml](acceptance/data-workspace-046.toml). Chart lock: [chart-interaction-046.toml](acceptance/chart-interaction-046.toml). Form kinds stay [type-form-inventory-044.toml](acceptance/type-form-inventory-044.toml). Catalog entry stays [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). Reserve `HED-BUNDLE-*` names in docs only. Locked answers: [RFC-0073 Resolved questions (D-079)](rfcs/RFC-0073-PACKAGE-NATIVE-WORKFLOWS.md#resolved-questions-d-079). Stage 0 / contract refine changes contracts only; a tracking issue is still required before Stage 1. Does **not** implement RFC-0073 runtime, RFC-0074, add 0.46 runtime symbols, rebase 0.47, or change package versions. |
| D-080 | Accepted | Phase **0.48** owns **first-class HTMX extension integration** under [RFC-0075](rfcs/RFC-0075-HTMX-EXTENSION-INTEGRATION.md). Original Stage 0 planning baseline was Published in-tree **`v0.46.0`**; **D-083** rebases the living/planning baseline to Published in-tree **`v0.47.0`**. Verified **`v0.47.0`** is the hard Stage 1 prerequisite and cut baseline; target is **`v0.48.0`**, with no Stage 0 runtime/version claim. Add a closed extension catalog and declaration set; demand-driven pinned local assets with activation/CSP/manifest/diagnostic parity; complete SSE, registered head-support, and cacheable-GET preload vertical slices; and an evidence-gated Idiomorph disposition. Preserve HTMX 2, polling fallback, server-authoritative `InteractionResult`/status targeting, OOB, loading, URL/selector/eval, adapter, and Web Component lifecycle contracts. Gate IDs: `EXTENSION-048`, `ASSET-048`, `SSE-048`, `HEAD-048`, `PRELOAD-048`, `MORPH-048`, `SECURITY-048`, `A11Y-048`, `BROWSER-048`, `PERF-048`, `ADAPTER-048`, `TOOLING-048`, `COMPAT-048`, `DOCS-048`, `REGRESS-048`, `PKG-048`. A tracking issue is required before Stage 1. Does **not** authorize arbitrary/CDN/community extensions, response-targets, multi-swap, loading-states, HTMX 1 compatibility, WebSocket wire-protocol replacement, client templates/JSON encoding/executable event headers, extension-dependent correctness, live-transport promotion, weakened security, `SR-021` closure, or `1.0`. Cut requires Verified 0.47, zero Deferred among non-disposition rows, and either Verified morph or an explicit Deferred/excluded morph disposition. |
| D-081 | Accepted | Phase **0.49** owns **FastAPI/Pydantic convergence** under [RFC-0076](rfcs/RFC-0076-FASTAPI-PYDANTIC-CONVERGENCE.md). Original Stage 0 planning baseline was Published in-tree **`v0.46.0`**; **D-084** rebases the living/planning baseline to Published in-tree **`v0.48.0`**. Verified **`v0.48.0`** is the hard Stage 1 prerequisite and cut baseline; target is **`v0.49.0`**, with no Stage 0 runtime/version claim. Add explicit handler/response dependency lifetimes; selective native query/header/cookie/form model binding with expanded fallback; sanitized validation/serialization TypeSchema projections; tagged wire unions; preserved router provenance; typed OpenAPI responses/media/callbacks/webhooks/security/scopes; strict content-type evidence; measured cached validation adapters; per-package settings disposition; and quarantined partial-validation/missing-state/FailFast research. Preserve direct FastAPI, structural binding, SafeUrl/security, HTML, Flask/Django, catalogs/manifests, and explicit rollback. Gate IDs: `LIFETIME-049`, `BINDING-049`, `SCHEMA-049`, `UNION-049`, `ROUTER-049`, `OPENAPI-049`, `SECURITY-049`, `ADAPTER-VALIDATION-049`, `SETTINGS-049`, `RESEARCH-049`, `A11Y-049`, `PERF-049`, `COMPAT-049`, `DOCS-049`, `REGRESS-049`, `PKG-049`. Tracking [#380](https://github.com/eddiethedean/hedron/issues/380). Does **not** authorize FastAPI alpha router hooks, SerializeAsAny at protected boundaries, serialization context as authorization, validate_call handler runtime, computed writable fields, Pydantic URLs replacing SafeUrl, FastAPI Cloud, automatic OAuth/webhook delivery, experimental Pydantic APIs in Supported surfaces, inferred transactions/background authority, `SR-021` closure, or `1.0`. Cut requires Verified 0.48, zero Deferred among non-disposition rows, and explicit settings/research dispositions. |
| D-082 | Accepted | **0.47 Stage 0 contract refine against Published in-tree `v0.46.0`.** Rebases D-078/RFC-0074 planning onto the real 0.15 `hedron.Map`/`sanitize_geojson`/`HED-MAP-0001`–`0004` seams, 0.43 handles, 0.44 `TypeSchema`, 0.45 catalog/manifest/projection, and 0.46 `FeatureBundle`/`ChartInteraction` pattern without reopening D-078 maps authority, gate IDs, RFC-0074, or 0.48/0.49 (D-080/D-081) ownership. Living/planning baseline becomes Published in-tree **`v0.46.0`**; cut target stays **`v0.47.0`** / independent Beta **`hedron-maps` `0.1.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.46 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#334` assets. Consume shipped core `Map`, sanitizer `GeoJSONLayer`, `MarkerSpec`, `sanitize_geojson`, `tile_allowlist` (core `Map` has **no** OSM default), `MAP_VIEWPORT_TRIGGER` (`map.viewport`), `FragmentHandle[BindT, ContentT]`, `ActionHandle[InputT, ResultT]`, `BoundFragment[ContentT]`, `Patch[ContentT]`, `BaseHandleDescriptor`, `descriptor_fingerprint` (does **not** hash `effect` or `extensions`), `TypeSchema` under **`hedron.type`**, `Hedron.include_component` / `include_feature` / `interactions`, `compile_interaction_catalog` / `seal_app_catalog` after `seal_registry`, `InteractionCatalog` / `CatalogEntry` / `PackageProjection`, `FeatureBundle` (not an executor), and `AppScenario`. `MapInteraction` in `hedron-maps` is the ChartInteraction analog: untrusted Pydantic event payloads to a registered `ActionHandle` plus `Refreshes`/`Updates`. Do **not** reuse `ChartInteraction` or charts’ `[data-hedron-chart="maplibre"]` host as the Supported maps renderer. Portable `MapSpec`/`MapPlan`/`compile_map` live in `hedron-maps` (depends on `hedron-core` only; no FastAPI/MapLibre/sqlite/network in portable models). MBTiles archive routes owned by flagship/adapters. Flask/Django stay `projection_adapter` stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) and [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Spec/plan lock: [map-spec-plan-047.toml](acceptance/map-spec-plan-047.toml). Provider/policy lock: [map-provider-policy-047.toml](acceptance/map-provider-policy-047.toml). Offline lock: [map-offline-047.toml](acceptance/map-offline-047.toml). Interaction/compat lock: [map-interaction-compat-047.toml](acceptance/map-interaction-compat-047.toml). Catalog entry stays [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). Chart interaction remains the pattern only ([chart-interaction-046.toml](acceptance/chart-interaction-046.toml)). Reserve `HED-MAP-SPEC-*` / `HED-MAP-SOURCE-*` / `HED-MAP-STYLE-*` / `HED-MAP-POLICY-*` / `HED-MAP-OFFLINE-*` / `HED-MAP-RUNTIME-*` / `HED-MAP-EVENT-*` names in docs only; keep `HED-MAP-0001`–`0004`. Locked answers: [RFC-0074 Resolved questions (D-082)](rfcs/RFC-0074-FIRST-CLASS-MAPS.md#resolved-questions-d-082). Stage 0 / contract refine changes contracts only; a tracking issue is still required before Stage 1. Does **not** implement RFC-0074 runtime, RFC-0075/0076, add 0.47 runtime symbols, scaffold `hedron-maps`, pin MapLibre version, invent numeric limits, rebase 0.48/0.49, or change package versions. |
| D-083 | Accepted | **0.48 Stage 0 contract refine against Published in-tree `v0.47.0`.** Rebases D-080/RFC-0075 planning onto the real 0.10 `ExtensionAsset`/`known_extensions()`/`inject_htmx_extensions` seams, experimental SSE/`HX-Preloaded` helpers, HDJ `ExtensionEvidence`, `safe_hx_swap`, 0.43 handles, 0.44 `TypeSchema`, 0.45 catalog/manifest/projection, 0.46 `FeatureBundle`, and 0.47 `hedron-map` lifecycle without reopening D-080 extension authority, gate IDs, RFC-0075, or 0.49 (D-081) ownership. Living/planning baseline becomes Published in-tree **`v0.47.0`**; cut target stays **`v0.48.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.47 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#350` assets. Consume shipped `ExtensionAsset` (htmx-ext-sse **2.2.2**, htmx-ext-head-support **2.0.2**), `inject_htmx_extensions`, `SseResponse` / `sse_response` / `job_status_sse_response` / `extension_script_tags`, `SseEvent` / `encode_sse`, `NavigationPreloadPolicy` / `PreloadDecision` / `decide_preload` / `HX-Preloaded`, `safe_hx_swap` (morph not admitted), HDJ `ExtensionEvidence` (`extension_id="sse"`; `hx-ext` never installs), `FragmentHandle[BindT, ContentT]`, `ActionHandle[InputT, ResultT]`, `BoundFragment[ContentT]`, `Patch[ContentT]`, `BaseHandleDescriptor`, `descriptor_fingerprint` (does **not** hash `effect` or `extensions`), `TypeSchema` under **`hedron.type`**, `Hedron.include_component` / `include_feature` / `interactions`, `compile_interaction_catalog` / `seal_app_catalog` after `seal_registry`, `InteractionCatalog` / `CatalogEntry` / `PackageProjection`, `FeatureBundle` (not an executor), and `AppScenario`. Public ids are `sse` / `head-support` / `preload` / conditional `morph`; `ExtensionAsset.name` stays `htmx-ext-*`. `Page.htmx_extensions` is additive in `hedron-core`; unset keeps the 0.47 pair as a compatibility default with a diagnostic; empty opts out; `preload`/`morph` never ride the default. `SseRegion`/`SseTrigger` are core HTML components. Preload is a GET authoring value, not a type named `Preload`. No new package. Flask/Django stay `projection_adapter` stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) and [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Catalog lock: [htmx-extension-catalog-048.toml](acceptance/htmx-extension-catalog-048.toml). Asset lock: [htmx-asset-activation-048.toml](acceptance/htmx-asset-activation-048.toml). Slice lock: [htmx-sse-head-preload-048.toml](acceptance/htmx-sse-head-preload-048.toml). Morph/compat lock: [htmx-morph-compat-048.toml](acceptance/htmx-morph-compat-048.toml). Catalog entry stays [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). Keep `HED-HTMX-0001`/`0002` and `HED-JINJA-0030`; reserve `HED-EXT-*` in docs only. Do not reopen `polling_only`. Locked answers: [RFC-0075 Resolved questions (D-083)](rfcs/RFC-0075-HTMX-EXTENSION-INTEGRATION.md#resolved-questions-d-083). Stage 0 / contract refine changes contracts only; a tracking issue is still required before Stage 1. Does **not** implement RFC-0075 runtime, RFC-0076, add 0.48 runtime symbols, vendor preload.js/Idiomorph, pin new asset digests, invent numeric limits, rebase 0.49, or change package versions. |
| D-084 | Accepted | **0.49 Stage 0 contract refine against Published in-tree `v0.48.0`.** Rebases D-081/RFC-0076 planning onto the real 0.43 handles, 0.44 `TypeSchema` v1 / `BindingPlan` / `PydanticBindingAdapter` / `apply_modeled_signature`, 0.45 catalog/manifest/projection, 0.46 `FeatureBundle`, 0.48 `Page.htmx_extensions` / `SseRegion` / experimental SSE helpers, `HedronRouter` / `install_openapi` / `operation_id_for`, and current FastAPI `Depends(scope=)` / Workbench argparse loaders without reopening D-081 gate IDs, RFC-0076, 0.48, `polling_only`, `MORPH-048`, or a later phase. Living/planning baseline becomes Published in-tree **`v0.48.0`**; cut target stays **`v0.49.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.48 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#373` assets. Hedron `DependencyLifetime.HANDLER` compiles to FastAPI `Depends(scope="function")`; `RESPONSE` compiles to `Depends(scope="request")`. `BindingPlan` stays the 0.43 structural plan; `BoundaryBindingPlan` is the new strategy record. TypeSchema v2 is additive under `hedron.type` and does not bump `TYPE_SCHEMA_VERSION` in Stage 0. FailFast belongs to `RESEARCH-049`, not `ADAPTER-VALIDATION-049`. Settings candidates are only `fastapi-workbench` / `hedron-workbench` / `hedron-posit` — not `hedron.config.HedronSettings`. Flask/Django stay `projection_adapter` stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) and [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Lifetime lock: [fastapi-lifetime-049.toml](acceptance/fastapi-lifetime-049.toml). Binding lock: [fastapi-binding-049.toml](acceptance/fastapi-binding-049.toml). TypeSchema v2 lock: [typeschema-v2-049.toml](acceptance/typeschema-v2-049.toml). Unions/OpenAPI lock: [fastapi-unions-openapi-049.toml](acceptance/fastapi-unions-openapi-049.toml). Settings/research lock: [fastapi-settings-research-049.toml](acceptance/fastapi-settings-research-049.toml). Type payload stays [type-schema-044.toml](acceptance/type-schema-044.toml). Catalog entry stays [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). Keep `HED-TYPE-0001`–`0010`; reserve `HED-FP-*` in docs only. Tracking [#380](https://github.com/eddiethedean/hedron/issues/380). Locked answers: [RFC-0076 Resolved questions (D-084)](rfcs/RFC-0076-FASTAPI-PYDANTIC-CONVERGENCE.md#resolved-questions-d-084). Stage 0 / contract refine changes contracts only. Does **not** implement RFC-0076 runtime, add 0.49 runtime symbols, install `pydantic-settings`, invent numeric limits, widen the Supported FastAPI matrix, or change package versions. |
| D-085 | Accepted | Phase **0.50** owns **Explorer architecture and operator-grade development tooling** under [RFC-0077](rfcs/RFC-0077-EXPLORER-ARCHITECTURE.md). Planning baseline is Published in-tree **`v0.49.1`**; **D-086** names the real 0.49.1 Explorer seams. Verified **`v0.49.1`** is the hard Stage 1 prerequisite and cut baseline; target is **`v0.50.0`**, with no Stage 0 runtime/version claim. Decompose `explorer_router` into portable services plus a thin HTTP transport; add versioned `ExplorerProvider` in `hedron-core` without removing `ExplorerPanelMeta` / `register_explorer_panel`; replace silent table slices with search/filter/pagination or truncation diagnostics; unify CLI `inspect`/`graph`/`check` with Explorer HTML/JSON when `hedron-explorer` is installed, reusing `diagnostics_to_sarif`; add a bounded interaction laboratory over today's simulate allowlists and a read-only package-health slice that is not `hedron package doctor`. Preserve frozen `/hedron-explorer/` mount, `explorer="off"|"development"|"secured"`, production refusal of `development`, and CSRF/simulate allowlists. Gate IDs: `ARCH-050`, `PROVIDER-050`, `CONSUME-050`, `QUERY-050`, `DIFF-050`, `LAB-050`, `HEADLESS-050`, `ECOSYSTEM-050`, `SECURITY-050`, `PRIVACY-050`, `A11Y-050`, `BROWSER-050`, `PERF-050`, `RESILIENCE-050`, `DOCS-050`, `COMPAT-050`, `REGRESS-050`, `PKG-050`. Tracking [#501](https://github.com/eddiethedean/hedron/issues/501). Companion authoring [#496](https://github.com/eddiethedean/hedron/issues/496)–[#500](https://github.com/eddiethedean/hedron/issues/500) / [#502](https://github.com/eddiethedean/hedron/issues/502) / [#503](https://github.com/eddiethedean/hedron/issues/503) are bound to this phase and do not reopen Explorer gate IDs. Does **not** authorize a production-default Explorer, live SSE/WS traces (`EXPLORER-10-001` stays Deferred on `0.10.x`), ATAG workspace graduation (`EXPLORER-019`), Flask/Django `explorer_router` mount, durable cross-process audit (`REV-026-003`), Node for application authors, `SR-021` closure, or `1.0`. Cut requires Verified 0.49 and zero Deferred among 0.50-owned non-disposition rows. |
| D-086 | Accepted | **0.50 Stage 0 contract refine against Published in-tree `v0.49.1`.** Rebases D-085/RFC-0077 planning onto the real `hedron.app.explorer` mount (`ExplorerMode` `off`/`development`/`secured`, `resolve_explorer_mode` / `mount_explorer_if_enabled` / `install_explorer_bridges`, prefix `/hedron-explorer`, `RISK_EXPLORER_DEVELOPMENT`), `hedron_explorer.router.explorer_router` (~1,366 lines; `explorer_guards` 120/60s), `ExplorerPanelMeta` / `register_explorer_panel` / `get_explorer_panels`, frozen HTML/JSON paths, `_SIMULATE_KEYS`, CSRF `hedron_csrf` / `X-CSRF-Token` / `X-Hedron-CSRF`, silent caps (`[:200]`, a11y `[:40]`, audit `[:20]`, `CacheTrace.recent(50)`, `_AUDIT` maxlen 200, `_TRACE` maxlen 100), CLI `inspect`/`graph`/`check`/`routes` (no `hedron_explorer` import today), and `diagnostics_to_sarif` without reopening D-085 gate IDs, RFC-0077, 0.49, `polling_only`, `MORPH-048`, `EXPLORER-10-001`, `EXPLORER-019`, `REV-026-003`, or a later phase. Living/planning baseline becomes Published in-tree **`v0.49.1`**; cut target stays **`v0.50.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.49 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#380` assets. `ExplorerProvider` is a portable `hedron-core` protocol (no FastAPI); HTTP/HTML stay in `hedron-explorer`; CLI may call shared services only when `hedron-explorer` is installed. `ExplorerPanelMeta` stays; provider is additive. Thin `router.py` is a module-boundary target plus golden route map, not permission to drop routes. Flask/Django currently hard-set `explorer_mode="off"`; 0.50 read-only adapters are labeled registry/catalog/CLI consumption, not a WSGI mount. HEADLESS-050 reuses `hedron check --format sarif`; do not add a second SARIF writer. LAB-050 executes only `/api/simulate`, `/api/click-preview`, and `/api/element-simulate`. Package health is not `hedron package doctor` (0.53). Flask/Django stay `projection_adapter` stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) and [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Architecture lock: [explorer-architecture-050.toml](acceptance/explorer-architecture-050.toml). Provider lock: [explorer-provider-050.toml](acceptance/explorer-provider-050.toml). Query lock: [explorer-query-050.toml](acceptance/explorer-query-050.toml). Diff lock: [explorer-diff-050.toml](acceptance/explorer-diff-050.toml). Lab lock: [explorer-lab-050.toml](acceptance/explorer-lab-050.toml). Headless lock: [explorer-headless-050.toml](acceptance/explorer-headless-050.toml). Catalog entry stays [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). TypeSchema v2 stays [typeschema-v2-049.toml](acceptance/typeschema-v2-049.toml). HTMX catalog stays [htmx-extension-catalog-048.toml](acceptance/htmx-extension-catalog-048.toml). Reserve `HED-EXPLORER-*` in docs only. Tracking [#501](https://github.com/eddiethedean/hedron/issues/501). Locked answers: [RFC-0077 Resolved questions (D-086)](rfcs/RFC-0077-EXPLORER-ARCHITECTURE.md#resolved-questions-d-086). Stage 0 / contract refine changes contracts only. Does **not** implement RFC-0077 runtime, split `router.py`, add 0.50 runtime symbols, invent numeric perf budgets, rebase 0.51–0.53, or change package versions. |
| D-087 | Accepted | Phase **0.51** owns **curated extras depth, browser lifecycle, and experimental-UI disposition** under [RFC-0078](rfcs/RFC-0078-CURATED-EXTRAS-LIFECYCLE.md). Planning baseline is Published in-tree **`v0.50.3`**; **D-088** names the real 0.50.3 extras seams. Verified in-tree **`v0.50.3`** is the hard Stage 1 prerequisite and cut baseline; target is **`v0.51.0`**, with no Stage 0 runtime/version claim. Inventory every extras feature as Supported/Experimental/Deprecated/removed; add versioned `ExtrasFeature` in `hedron-extras` without replacing `FeatureBundle` or `InteractionCatalog`; deepen workbench/image/lifecycle/a11y/security/browser evidence; keep optional extras non-transitive. `CodeEditor` / `TerminalView` / `Joystick` / `DeviceBridge` remain Experimental behind `hedron[experimental-ui]` and do not graduate. `BrowserPythonSandbox` remains Experimental and is not in the Supported `hedron[extras]` inventory (Stage 1 makes default registration opt-in). Gate IDs: `INVENTORY-051`, `DESCRIPTOR-051`, `WORKBENCH-051`, `DATA-051`, `IMAGE-051`, `INPUT-051`, `LIFECYCLE-051`, `BROWSER-051`, `SECURITY-051`, `SUPPLY-051`, `A11Y-051`, `VISUAL-051`, `ECOSYSTEM-051`, `DOCS-051`, `PKG-051`, `REGRESS-051`. Tracking [#507](https://github.com/eddiethedean/hedron/issues/507). Companion authoring [#504](https://github.com/eddiethedean/hedron/issues/504)–[#506](https://github.com/eddiethedean/hedron/issues/506) is bound to this phase and does not reopen extras gate IDs. Does **not** authorize CodeMirror 6 graduation, Experimental UI in Supported `hedron[extras]`, 0.52 conformance/runtimes, 0.53 notebook/sim/sample-kit / `hedron package doctor`, `polling_only` reopen, `MORPH-048` reopen, `SR-021` closure, or `1.0`. Cut requires Verified in-tree 0.50 and zero Deferred among 0.51-owned extras rows. |
| D-088 | Accepted | **0.51 Stage 0 contract refine against Published in-tree `v0.50.3`.** Rebases D-087/RFC-0078 planning onto the real `hedron_extras.plugin` / `hedron_extras.experimental` entry points (`hedron_extras` vs `hedron_extras_experimental` with `depends_on=("hedron_extras",)`), `HEDRON_EXPERIMENTAL_UI` / explicit enable, `PluginMeta` / `PluginCapabilities` / `register_feature`, in-plugin `feature_specs` (`composition`, `workbench`, `image_tools`, `calendar`, `signature`, `typeahead`, `display`, `recipes`, `sandbox`), browser hosts (`hedron-extras-sandbox`, `hedron-extras-image-tools`, `hedron-extras-calendar`, `hedron-extras-signature`, `hedron-extras-typeahead`, `hedron-extras-composition`, experimental `hedron-extras-code-editor` / `hedron-extras-terminal`), optional extras (`json_editor`, `data_explorer`, `chart_workbench`, `image_tools`, `calendar`, `signature`, `typeahead`, `sandbox`, `code_editor`, `terminal`, `joystick`, `experimental-ui`, `all`), EXTRAS-025 quarantine, Explorer panel `hedron-extras-features`, projection `hedron.extras`, and `HED-EXTRAS-` without reopening D-087 gate IDs, RFC-0078, 0.50, `polling_only`, `MORPH-048`, `SR-021`, 0.52, 0.53, or a later phase. Living/planning baseline becomes Published in-tree **`v0.50.3`**; cut target stays **`v0.51.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.50.3 is enough predecessor evidence; do not block a later Stage 1 on PyPI/GitHub `#501` assets. `ExtrasFeature` is package-owned in `hedron-extras` and consumes `feature_specs`; it is not a `hedron-core` catalog and does not replace `FeatureBundle`. Sandbox `feature_specs` `stability: beta` is an honesty defect: lock Experimental and keep it out of Supported `hedron[extras]`. Recipes stay `recipe` maturity unless the inventory promotes a subset. PKG-051 upgrade source is **0.50**, not 0.49. Flask/Django stay `projection_adapter` stacked on [adapter-disposition-044.toml](acceptance/adapter-disposition-044.toml) and [host-portable-facts-045.toml](acceptance/host-portable-facts-045.toml). Inventory lock: [extras-capability-inventory-051.toml](acceptance/extras-capability-inventory-051.toml). Descriptor lock: [extras-descriptor-051.toml](acceptance/extras-descriptor-051.toml). Experimental lock: [extras-experimental-disposition-051.toml](acceptance/extras-experimental-disposition-051.toml). Workbench lock: [extras-workbench-051.toml](acceptance/extras-workbench-051.toml). Lifecycle lock: [extras-lifecycle-051.toml](acceptance/extras-lifecycle-051.toml). Companion lock: [extras-companion-authoring-051.toml](acceptance/extras-companion-authoring-051.toml). Catalog entry stays [catalog-entry-045.toml](acceptance/catalog-entry-045.toml). Quarantine stays [extras-quarantine-025.toml](acceptance/extras-quarantine-025.toml). Keep `HED-EXTRAS-`; reserve `HED-EXTRAS-FEATURE-*` in docs only. Tracking [#507](https://github.com/eddiethedean/hedron/issues/507). Locked answers: [RFC-0078 Resolved questions (D-088)](rfcs/RFC-0078-CURATED-EXTRAS-LIFECYCLE.md#resolved-questions-d-088). Stage 0 / contract refine changes contracts only. Does **not** implement RFC-0078 runtime, move sandbox registration, add 0.51 runtime symbols, invent numeric limits, rebase 0.52–0.53, or change package versions. |
| D-089 | Accepted | Phase **0.52** owns **cross-language conformance authority** and **HedronPosit deployment lifecycle** under [RFC-0079](rfcs/RFC-0079-CONFORMANCE-AUTHORITY-POSIT-LIFECYCLE.md). Planning baseline is Published in-tree **`v0.51.2`**; **D-090** names the real 0.51.2 conformance and Posit seams. Verified in-tree **`v0.51.2`** is the hard Stage 1 prerequisite and cut baseline; target is **`v0.52.0`**, with no Stage 0 runtime/version claim. Two workstreams, one cut: (A) versioned portable-subset authority extending `hedron-portable-1`, fixture compiler, Node/Java independent install artifacts, differential/platform evidence; (B) cookie registry, `PositContext`, hands-off URL adaptation, deployment-matrix check, proactive diagnostics, and named-route query/fragment parity. Gate IDs: `PROTOCOL-052`, `PROFILE-052`, `FIXTURE-052`, `NEGATIVE-052`, `RUNTIME-052`, `DIFF-052`, `SECURITY-052`, `SANDBOX-052`, `REPORT-052`, `CI-052`, `COMPAT-052`, `PLATFORM-052`, `COOKIE-052`, `CONTEXT-052`, `HANDSOFF-052`, `MATRIX-052`, `PDIAG-052`, `ROUTEURL-052`, `DOCS-052`, `AUTHOR-052`, `PKG-052`, `SUPPLY-052`, `REGRESS-052`. Tracking [#522](https://github.com/eddiethedean/hedron/issues/522). Companions [#508](https://github.com/eddiethedean/hedron/issues/508)–[#513](https://github.com/eddiethedean/hedron/issues/513) are bound to this phase and do not reopen a Supported Connect cookie bridge (`drop_supported` remains). Does **not** authorize full non-Python Hedron ports, browser runtimes for Node/Java, restoring the Supported Connect bridge, reopening `polling_only` / `MORPH-048` / Explorer 0.50 / 0.51 extras / `SR-021`, owning 0.53–0.54, or `1.0`. Cut requires Verified in-tree 0.51.2 and zero Deferred among 0.52-owned non-disposition rows. |
| D-090 | Accepted | **0.52 Stage 0 contract refine against Published in-tree `v0.51.2`.** Rebases D-089/RFC-0079 planning onto the real 0.51.2 conformance seams (`CONTRACT_VERSION = "hedron-portable-1"`, `FIXTURE_VERSION`, `Capability`, `load_bundled_fixtures()`, CLI `run`/`list`/`schema`/`compat`, top-level `fixtures/*.json` default corpus, tooling-grade `hedron-runtime-node`/`hedron-runtime-java`) and HedronPosit helpers (`HedronPosit` `href`/`href_for`, `redirect`/`redirect_for`, `browser_url`/`browser_url_for`, `external_url`/`durable_url`, `cookie_path_for_mount`, `workbenchify`, `ConnectCookieMode.NATIVE`, CLI `hedron-posit check`/`run`/`doctor`) without reopening D-089 gate IDs, RFC-0079, 0.51 extras, Explorer 0.50, `polling_only`, `MORPH-048`, `SR-021`, Supported Connect bridge (`drop_supported`), 0.53, 0.54, or a later phase. Living/planning baseline becomes Published in-tree **`v0.51.2`**; cut target stays **`v0.52.0`**; no Stage 0 tip/runtime/version bump. In-tree Verified 0.51.2 is enough predecessor evidence; do not block a later Stage 1 on a later 0.51.x PyPI tip. Portable subset lock: Node/Java evaluate declared profiles only — no FastAPI, browser, or complete Hedron. Planned Stage 1 symbols include `PositContext` / `posit_for(request)` and `hedron-posit check --matrix`. PKG-052 upgrade source is **0.51** (`v0.51.2`), not 0.50. Inventory lock: [conformance-capability-inventory-052.toml](acceptance/conformance-capability-inventory-052.toml). Profile lock: [conformance-profile-052.toml](acceptance/conformance-profile-052.toml). Posit lock: [posit-lifecycle-052.toml](acceptance/posit-lifecycle-052.toml). Upgrade fixtures: [upgrade-fixtures-052.md](acceptance/upgrade-fixtures-052.md). Tracking [#522](https://github.com/eddiethedean/hedron/issues/522). Locked answers: [RFC-0079 Resolved questions (D-090)](rfcs/RFC-0079-CONFORMANCE-AUTHORITY-POSIT-LIFECYCLE.md#resolved-questions-d-090). Stage 0 / contract refine changes contracts only. Does **not** implement RFC-0079 runtime, add 0.52 runtime symbols, invent numeric limits, rebase 0.53–0.54, or change package versions. |
| D-091 | Accepted | Phase **0.53** owns **application DX contracts** under [RFC-0080](rfcs/RFC-0080-APPLICATION-DX-CONTRACTS.md): ordered application assets, version-aware actionable diagnostics, structured route/effect export, long-running-operation workflow composition, catalog test generation, semantic theming conformance, curated API discovery, and installed-fleet doctor. Planning and required predecessor baseline is Published/Verified in-tree **`v0.52.0`**; target is **`v0.53.0`**. Gate IDs: `ASSET-053`, `DIAG-053`, `ROUTE-053`, `WORKFLOW-053`, `TESTGEN-053`, `THEME-053`, `DISCOVER-053`, `FLEET-053`, `DOCS-053`, `PKG-053`, `REGRESS-053`. Tracking issue packet [#514](https://github.com/eddiethedean/hedron/issues/514)–[#521](https://github.com/eddiethedean/hedron/issues/521) is bound directly to the phase. Does **not** replace 0.54 external-author `hedron package doctor`, reopen `polling_only` / `MORPH-048` / `SR-021`, change adapter dispositions, or schedule `1.0`. |
| D-092 | Accepted | **0.53 Stage 0 contract refine against Published in-tree `v0.52.0`.** Rebases D-091/RFC-0080 onto shipped seams: `inject_page_assets` / `AssetRef` / registry `AssetMeta`; `Diagnostic` / `DiagnosticSeverity` / `Suppression` / JSON-text-SARIF serializers and `hedron check`; registry `RouteMeta`, `routes_json`, and CLI `hedron routes`; `JobBackend` / `JobHandle` / `JobStatus` / `JobState`, `job_status_interaction`, and SSE job events; `InteractionCatalog` / `CatalogEntry` / sealed app catalogs; `Theme`, required semantic tokens, element style contracts, and `default_styles=False`; top-level `hedron.__all__`; Explorer read-only package health. Living/planning baseline stays **`v0.52.0`** and target stays **`v0.53.0`**; no Stage 0 runtime/version bump. Application assets extend the typed pipeline and never authorize arbitrary response rewriting. Route export is metadata only and never executes handlers. Workflow composition layers on `JobBackend` and does not reopen `polling_only`. Generated tests are reviewable artifacts, not runtime-generated executable code from untrusted catalogs. Fleet doctor is installed-application triage; 0.54 owns external-package author validation. Locks: [application-dx-inventory-053.toml](acceptance/application-dx-inventory-053.toml), [application-assets-053.toml](acceptance/application-assets-053.toml), [application-contracts-053.toml](acceptance/application-contracts-053.toml), [application-tooling-053.toml](acceptance/application-tooling-053.toml). Tracking stays the bound [#514](https://github.com/eddiethedean/hedron/issues/514)–[#521](https://github.com/eddiethedean/hedron/issues/521) packet. Stage 0 changes contracts only; it does **not** add 0.53 symbols, invent numeric limits, rebase 0.54, or change package versions. |

Post-cut note on D-085 / D-086: Stage 1 shipped in-tree **`v0.50.0`**. Living tip is
`v0.50.3` (PyPI latest `v0.50.1`; 0.50.3 tag deferred). Silent caps listed in D-086 are the **0.49.1 planning baseline**; QUERY-050
forbids silent slices after cut. CLI now imports explorer services when installed.
[#501](https://github.com/eddiethedean/hedron/issues/501) stays open for tag/PyPI. Do not
rewrite the Stage 0 D-086 contract above.

Post-0.29 hardening amendment to D-057: construction-time cookie scoping remains
preferred, while the outer adapter may repair only Hedron-owned root cookies
from a validated request mount. The pre-bound parent may also exec Uvicorn
reload or multi-worker supervision with its inherited listener; the two modes
are mutually exclusive. Unknown cookies and arbitrary response bodies remain
outside the automatic adaptation contract.

Amendment to D-065 (high-severity remediations): Phase **0.37** also owns the open
high-severity defects
[#230](https://github.com/eddiethedean/hedron/issues/230),
[#231](https://github.com/eddiethedean/hedron/issues/231),
[#232](https://github.com/eddiethedean/hedron/issues/232),
[#233](https://github.com/eddiethedean/hedron/issues/233),
[#234](https://github.com/eddiethedean/hedron/issues/234),
[#235](https://github.com/eddiethedean/hedron/issues/235),
[#236](https://github.com/eddiethedean/hedron/issues/236), and
[#237](https://github.com/eddiethedean/hedron/issues/237).
Issue bodies remain normative for `REGRESS-037` and the mapped form/HTMX/validity/action
gates in [RELEASE_0_37](acceptance/RELEASE_0_37.md). Cut `v0.38.0` requires those issues
closed. This does not add a tenth gate ID, does not reopen 0.37 non-goals, and does not
move later Web Component phases. Post-cut follow-on
[#244](https://github.com/eddiethedean/hedron/issues/244) (`style=` / `vbscript:` /
`data:` in `render_element_markup`) closed on the same `HTMX-037` helper.

## Phase 0.7 entry blockers

Entry blockers for adapter implementation (COMPATIBILITY ranges, evidence ledgers, core ownership,
Explorer acyclic deps) are closed for the `v0.7.0` train. Remaining work is staged gates 0.7A–0.7F.

Changes to accepted decisions require an explicit superseding decision and affected RFC/API updates.
