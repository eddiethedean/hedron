# Canonical fixture

This fixture uses only the frozen 1.0 authoring names (`page`, `view`,
`action`, and plain `Hedron` configuration). It is scanned and imported by the
phase-1.0 fixture tests; it is deliberately free of 0.67 compatibility paths.
