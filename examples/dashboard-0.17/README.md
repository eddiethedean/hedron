# Phase 0.17 reference analytical app

Demonstrates AppShell navigation, InteractionGraph registration, and PropertyPatch with
full-fragment fallback. Optional notebook/MCP wiring is documented in the phase guides;
this sample stays dependency-light.

```bash
uv run uvicorn examples.dashboard-0.17.app:app --reload
```
